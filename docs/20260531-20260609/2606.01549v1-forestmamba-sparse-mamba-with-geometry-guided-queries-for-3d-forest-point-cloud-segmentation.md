---
title: "ForestMamba: Sparse Mamba with Geometry-guided Queries for 3D Forest Point Cloud Segmentation"
title_zh: ForestMamba：基于几何引导查询的稀疏Mamba用于三维森林点云分割
authors: "Trung Thanh Nguyen, Tuan-Anh Vu, Duc Viet Le, Yasutomo Kawanishi, Takahiro Komamizu, Ichiro Ide, Teja Kattenborn"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01549v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 三维森林场景的点云语义和实例分割
tldr: 森林LiDAR点云分割面临大数据量、不规则采样、重叠冠层等挑战，现有Transformer的平方复杂度难以扩展，且通用建模忽略森林结构先验。ForestMamba通过垂直优先序列化编码、多尺度冠层高度模型引导的查询初始化和基于Mamba的线性复杂度解码，利用森林结构提升树冠分离。在七个森林区域上，ForestMamba分割精度优于主流方法，同时推理速度提升3倍，GPU内存占用降低2.3倍。该工作展示了状态空间模型与领域先验结合在生态遥感中的潜力，为高效森林监测提供了新工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01549v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1310, \"height\": 869, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01549v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1188, \"height\": 317, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01549v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1323, \"height\": 1027, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01549v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1344, \"height\": 1220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01549v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1344, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01549v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 578, \"label\": \"Table\"}]"
motivation: 现有方法因二次复杂度和缺乏结构先验，难以高效处理大规模森林点云并分离重叠树冠。
method: 提出ForestMamba，通过垂直序列化编码、CHM引导查询和Mamba双路径解码，实现结构感知与线性复杂度。
result: 在七个森林区域，分割精度均超越Transformer等基线，推理提速3倍，内存降2.3倍。
conclusion: ForestMamba将森林先验与Mamba结合，为大规模点云分割提供高效精准方案，拓展了生态遥感应用。
---

## 摘要
基于AI的地面和无人机LiDAR点云的语义与实例分割正成为一种变革性方法，可将森林复杂的三维结构转化为可操作的信息，用于森林监测和生物多样性评估。然而，森林LiDAR场景仍极具挑战性，原因在于其数据量大、采样密度不规则、冠层结构重叠复杂以及地理变异性。现有的基于稀疏卷积或Transformer的方法取得了可喜的成果，但存在两个关键局限：注意力的二次复杂度难以扩展到大型森林场景；通用上下文建模未能利用森林结构先验，限制了复杂区域中树木的分离。为了解决这些挑战，我们提出了ForestMamba，一种结构感知方法，它将特定于森林的先验融入特征编码、查询生成和查询细化中，同时用线性时间状态空间建模取代二次复杂度的注意力。首先，我们引入了一种具有垂直优先切片序列化的稀疏编码器，将稀疏体素组织成垂直连贯的序列，以实现高效的长程上下文建模。其次，我们提出了一种基于即时多尺度冠层高度模型（CHM）的几何引导查询初始化策略，其中冠层最大值提供了具有生态意义的查询种子，并通过最远点采样（FPS）进行补充，以覆盖林下树木。第三，我们设计了一个基于Mamba的查询解码器，该解码器将局部kNN体素聚合与空间双路径Mamba相结合，以线性计算复杂度进行查询细化。在七个森林区域上的大量实验表明，ForestMamba在两项分割任务中均一致优于现有基线，同时推理速度比基于Transformer的方法快3倍，GPU内存消耗低2.3倍。

## Abstract
AI-based semantic and instance segmentation of terrestrial and drone LiDAR point clouds is emerging as a transformative approach for converting the complex 3D structure of forests into actionable information for forest monitoring and biodiversity assessment. However, forest LiDAR scenes remain highly challenging due to their large data volumes, irregular sampling density, overlapping and complex canopy structure, and geographic variability. Existing methods based on sparse convolutions or Transformers achieve promising results, but suffer from two key limitations: Quadratic complexity of attention scales poorly to large forest scenes, and Generic context modeling does not exploit forest structural priors, limiting tree separation in complex regions. To address these challenges, we propose ForestMamba, a structure-aware method that incorporates forest-specific priors into feature encoding, query generation, and query refinement, while replacing quadratic attention with linear-time state-space modeling. First, we introduce a sparse encoder with vertical-priority slab serialization that organizes sparse voxels into vertically coherent sequences for efficient long-range context modeling. Second, we propose a geometry-guided query initialization strategy based on an on-the-fly multi-scale Canopy Height Model (CHM), where canopy maxima provide ecologically meaningful query seeds, supplemented by Farthest Point Sampling (FPS) to cover understory trees. Third, we design a Mamba-based query decoder that combines local kNN voxel aggregation with a spatial dual-path Mamba for query refinement with linear computational complexity. Extensive experiments across seven forest regions demonstrate that ForestMamba consistently outperforms existing baselines in both segmentation tasks, while achieving 3 times faster inference and 2.3 times lower GPU memory than Transformer-based methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**：森林覆盖了约 31% 的陆地表面，在碳循环和生物多样性中扮演核心角色。利用地面和无人机 LiDAR 进行的 3D 点云分割，是将复杂森林结构转化为生态监测信息的关键技术，需同时完成**语义分割**与**实例分割**两个子任务。
*   **核心挑战**：
    *   **数据规模与复杂性**：森林点云规模庞大（每公顷数亿点）、采样密度不规则、冠层重叠交织且存在严重类别不平衡。
    *   **现有方法的根本局限**：
        1.  **计算效率瓶颈**：基于 Transformer 的 SOTA 方法存在注意力的**二次计算复杂度**，推理慢、内存消耗高，难以扩展至大型场景。
        2.  **缺乏结构先验**：通用的上下文建模未利用树木垂直生长的结构先验，导致在密集重叠区域分离树冠时表现不佳。

*   **整体含义**：为解决上述问题，本文提出了 **ForestMamba**，一种结构感知的分割框架。其核心创新在于将**森林特有的垂直结构先验**融入特征编码和查询初始化，并用**线性复杂度的状态空间模型**取代二次复杂度的自注意力，旨在实现更精确、更高效的大规模森林场景理解。

### 2. 论文提出的方法论

*   **核心思想**：利用树木“地面-树干-树冠”的垂直层级结构，设计一个从特征提取到查询生成全程结构感知的框架，并通过 Mamba 模型实现线性复杂度的长程上下文建模。

*   **关键技术细节与流程**：
    1.  **结构感知的稀疏编码器**：
        *   **垂直优先切片序列化**：将 3D 体素按高度（垂直切片）排序，生成能体现垂直结构一致性的特征序列。
        *   **U-Net + Mamba 混合架构**：采用 U-Net 风格的稀疏卷积网络作为 backbone，并在每个编码器阶段后插入**Mamba 块**。Mamba 块处理经过序列化的体素特征，用线性复杂度捕捉垂直方向的长程依赖关系。

    2.  **几何引导的查询初始化**：
        *   **动机**：传统的可学习嵌入或 FPS 初始化未能显式反映森林的几何结构，会在密集区域产生冗余查询。
        *   **多尺度 CHM 峰值检测与特征池化**：
            *   通过辅助分支预测树体素，构建即时冠层高度模型（CHM）。
            *   在 CHM 上利用**基于异速生长方程的自适应窗口** $`w_r(h) = \lceil \alpha h^\beta / r \rceil`$ 检测局部峰值，作为具有生态意义的查询种子。
            *   以峰值点为中心，在圆柱体内聚合编码器特征，生成 CHM 引导的查询。
        *   **FPS 补充**：对 CHM 未能覆盖的林下或遮挡树木，利用辅助分支的判别嵌入进行最远点采样，生成补充查询。

    3.  **基于 Mamba 的查询解码器**：
        *   **局部 kNN 特征聚合**：每个查询只与其在 3D 空间中最近的 k 个体素进行注意力交互，避免与无关背景体素的计算，减少噪声。
        *   **空间双路径 Mamba**：
            *   将更新后的查询按其 3D 锚点位置进行序列化。
            *   为消除 Mamba 因果扫描的排序偏差，采用**自底向上和自顶向下**的双路序列化，通过同一 Mamba 模型处理后取均值融合，实现双向空间上下文建模。
        *   **迭代掩码预测**：解码器的每一层都预测实例掩码和目标分数，提供逐层辅助监督。

### 3. 实验设计

*   **数据集与场景**：在 **FOR-instanceV2** 数据集上进行评估，该数据集覆盖了 7 个地理分布广泛的森林区域（捷克、挪威、澳大利亚、新西兰、奥地利、法属圭亚那），包含寒带、温带和热带不同森林类型。

*   **评估基准**：
    *   **实例分割**：精确率 (P)、召回率 (R)、F1 分数、覆盖率 (Coverage)。
    *   **语义分割**：三个类别 的地面 (Ground)、木质 (Wood)、树叶 (Leaf) 的交并比 (IoU) 和平均 IoU (mIoU)。

*   **对比方法**：
    *   **传统/聚类方法**：TreeLearn、ForAINet。
    *   **通用 3D Transformer**：OneFormer3D。
    *   **SOTA 森林分割 Transformer**：ForestFormer3D。

### 4. 资源与算力

*   **文中未明确提及**具体训练时长、GPU 数量和型号。仅在模型复杂度和推理速度对比中，指出所有方法的推理均在 **NVIDIA RTX A6000 GPU** 上进行，平均超过 20 次运行。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **主要对比实验**：在 7 个不同森林区域的测试集上进行了全面对比。
    *   **模型复杂度分析**：对比了参数量、推理时间和 GPU 内存消耗。
    *   **消融实验**：系统地验证了编码器结构、查询初始化、解码器设计和训练策略四个方面的贡献，共计 7 项消融设置。
*   **实验充分性与客观性**：
    *   **充分性较高**：实验覆盖了多个分布广泛的真实森林数据集，对比方法涵盖了传统非深度学习、通用 3D 分割到 SOTA 森林专用模型，且包含了详细的消融研究，验证了方法的有效性。
    *   **客观性较好**：模型复杂度对比在相同设置和硬件上进行，主要指标为通用分割指标，消融实验设计控制得当。

### 6. 论文的主要结论与发现

*   ForestMamba 在七个森林区域的**加权平均 F1 分数 (83.4%) 和召回率均超越了所有基线方法**，尤其是在大型、结构复杂的森林中优势明显。
*   与 SOTA 方法 ForestFormer3D 相比，ForestMamba 的**推理速度提升约 3 倍，GPU 显存占用降低约 2.3 倍**。
*   消融实验证实了**各设计模块的有效性**，例如，多尺度 CHM 查询提升召回率，双路径 Mamba 消解读器排序偏差等。

### 7. 优点

*   **高效的线性复杂度**：首次在森林点云分割中引入状态空间模型，成功解决了 Transformer 的二次复杂度瓶颈，实现了效率与性能的平衡。
*   **创新的结构先验融合**：森林垂直结构先验贯穿特征序列化、查询初始化的全过程，具有明确的物理和生态意义。
*   **精巧的解码器设计**：局部 kNN 聚合和双路径 Mamba 的结合，既降低了计算量，又有效增强了查询与空间上下文的对齐能力。
*   **详尽的实验验证**：在多元化的森林数据集和全面的消融实验上证明了方法的优越性和各个组件的贡献。

### 8. 不足与局限

*   **对辅助任务的依赖**：几何引导的查询初始化高度依赖于辅助分支对“树/背景”体素的预测准确性，在多层、密闭的森林中，该预判的性能可能会退化。
*   **固定参数的泛化性**：CHM 峰值检测所依赖的异速生长参数 (α, β) 为固定经验值，虽能覆盖部分场景，但可能并非对所有森林类型都最优。
*   **编码器效率仍有空间**：虽然总推理速度大幅领先，但其编码器的运行时间高于 Transformer 基线，未来可进一步优化。
*   **应用范围限制**：方法目前专注于树冠分割，对林下层树木和特定下游生态任务（如生物量估算）的扩展待进一步研究。

（完）
