---
title: "DPNeXt: A Lightweight Multi-Scale Feature Fusion Framework for Efficient ViT-Based Multi-Task Dense Prediction"
title_zh: DPNeXt：一种面向高效ViT多任务密集预测的轻量级多尺度特征融合框架
authors: "Jehun Kang, Jungha Wang, Youngjun Hwang, David Hyunchul Shim"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16012v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 在多任务学习中解决语义分割与深度估计以实现3D场景理解，直接匹配需求4
tldr: "多任务学习结合语义分割与深度估计，视觉基础模型（VFM）虽提取鲁棒特征，但标准解码器DPT参数量大且效率低，成为瓶颈。DPNeXt提出精简多尺度融合解码器，采用双深度可分离倒置瓶颈和任务模块化，引入多任务边界引导策略以减轻负转移。在Cityscapes和NYUv2上，DPNeXt-S超越SOTA，参数较DPT减少78.6%，并在笔记本上实现最快推理；DPNeXt-B进一步提升性能。该方法高效利用VFM，推动资源受限场景下的多任务感知。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16012v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 972, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1832, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 607, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 897, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16012v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1751, \"height\": 1917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 898, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16012v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 905, \"height\": 463, \"label\": \"Table\"}]"
motivation: 视觉基础模型解码器效率低，多任务间负转移影响性能，缺乏轻量边界感知融合方案。
method: DPNeXt使用双深度可分离倒置瓶颈改进VFM特征融合，并引入对称边界监督MTBG策略。
result: "在Cityscapes和NYUv2上，DPNeXt-B达最佳分割与深度估计，DPNeXt-S参数减78.6%，笔记本推理最快。"
conclusion: DPNeXt通过融合解码与边界引导实现高效多任务密集预测，显著减少参数并提升速度，优于现有方法。
---

## 摘要
机器人感知系统中的多任务学习（MTL）通过整合语义分割和深度估计，支持全面的三维空间场景理解。虽然视觉基础模型（VFMs）越来越多地被用作鲁棒的特征编码器，但现有的解码策略构成了关键瓶颈。为解决这一问题，我们提出了DPNeXt，一种简化的多尺度特征融合解码器，是标准密集预测变换器（DPT）的高效替代方案。DPNeXt采用双深度可分离倒置瓶颈结构，通过以融合为中心的解码和独立的任务模块化，改善了对冻结VFMs的利用。为了进一步减轻任务间的负迁移，我们引入了多任务边界引导（MTBG）策略。与先前添加融合模块或门控的边界感知方法不同，MTBG施加对称的边界聚焦监督，以促进几何一致性，且无需额外标注或推理成本。在Cityscapes上的实验表明，DPNeXt-S优于先前最先进的MTL模型，而DPNeXt-B进一步提升了整体性能，并在对比方法中取得了最佳结果。在NYUv2上，DPNeXt-B同样在对比方法中实现了最佳的语义分割和深度估计结果，同时所需的可训练参数远少于先前的大规模MTL模型。与标准DPT相比，DPNeXt-S的可训练参数减少了78.6%，并在资源受限的笔记本电脑硬件上实现了对比模型中最快的推理速度。源代码、模型检查点和演示视频将在https://github.com/kangjehun/DPNeXt发布。

## Abstract
Multi-Task Learning (MTL) in robotics perception systems supports comprehensive 3D spatial scene understanding by integrating semantic segmentation and depth estimation. While Vision Foundation Models (VFMs) are increasingly adopted as robust feature encoders, existing decoding strategies present a critical bottleneck. To address this, we propose DPNeXt, a streamlined multi-scale feature fusion decoder and efficient alternative to the standard Dense Prediction Transformer (DPT). DPNeXt uses dual depthwise separable inverted bottlenecks to improve frozen VFM utilization through fusion-centric decoding and independent task modularization. To further mitigate negative inductive transfer between tasks, we introduce the Multi-Task Boundary Guidance (MTBG) strategy. Unlike prior boundary-aware methods that add fusion modules or gating, MTBG applies symmetric boundary-focused supervision to encourage geometric consistency without extra annotation or inference cost. Experiments on Cityscapes show that DPNeXt-S outperforms prior state-of-the-art (SOTA) MTL models, while DPNeXt-B further improves the overall performance and achieves the best results among the compared methods. On NYUv2, DPNeXt-B also achieves the best semantic segmentation and depth estimation results among the compared methods while requiring substantially fewer trainable parameters than prior large-scale MTL models. Compared with the standard DPT, DPNeXt-S reduces trainable parameters by 78.6% and achieves the fastest inference speed among the compared models on resource-constrained laptop hardware. The source code, model checkpoints, and a demo video will be made available at https://github.com/kangjehun/DPNeXt.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将以中文、使用 Markdown 格式，对该论文进行结构化、深入、客观的总结。

---

### **DPNeXt: 面向高效 ViT 多任务密集预测的轻量级多尺度特征融合框架 - 深入分析**

### 1. 论文的核心问题与整体含义

*   **核心问题**：在机器人三维场景理解中，多任务学习（MTL）通过共享编码器同时处理语义分割和深度估计，虽能提升效率，但其**解码器设计**成为关键瓶颈。尤其是当强大的视觉基础模型（VFM，如 DINOv2）作为冻结的编码器时，标准解码器（如密集预测变换器 DPT）存在参数冗余、计算效率低下的问题，且未能充分、高效地利用 VFM 提取的丰富特征。
*   **研究动机**：
    *   **解码器效率瓶颈**：现有解码策略需在精度与效率间平衡。简单的解码器性能不佳，而复杂的 Transformer 或大规模多尺度融合解码器计算负担重，且自定义算子缺乏硬件优化，导致实际推理速度慢。
    *   **多任务负转移**：同时学习语义分割和深度估计时，任务间可能相互干扰（负迁移），降低整体性能。现有缓解方法（如添加辅助分支或门控机制）会增加推理延迟或结构耦合。
*   **整体含义**：论文旨在提出一个**实用、高效的多任务密集预测框架**，使其能够在资源受限的边缘设备（如笔记本电脑）上实现快速、准确的 3D 场景理解，为下游机器人任务（如 3D 重建、自主导航）提供紧凑的特征表示。

### 2. 论文提出的方法论

核心思想是设计一个**以融合为中心、高度模块化的轻量级解码器（DPNeXt）**，并配合一种**零推理成本的任务平衡策略（MTBG）**，以最大化冻结 VFM 的潜力。

*   **核心组件1：DPNeXt 解码器**
    *   **设计哲学**：摒弃标准 DPT 中模拟 CNN 层次化的冗余通道扩张，采用纯卷积操作实现硬件友好的高效推理。灵感来源于现代卷积架构（如 ConvNeXt）的倒置瓶颈设计。
    *   **关键技术细节**：
        1.  **各向同性投影适配器（IPA）**：将 ViT 编码器各层提取的、维度各异的各向同性特征令牌，统一投影到一个共享的融合维度（`dfusion = 256`），并进行空间分辨率重排，形成特征金字塔，**避免通道扩张**。
        2.  **双深度可分离倒置融合（DDSIF）块**：作为核心融合单元，递归式地聚合多尺度特征（从深层到浅层）。每个 DDSIF 块包含两个阶段：
            *   **阶段一**：在融合维度上进行深度卷积空间滤波，再通过逐点卷积（扩展通道，扩张率 `r=2`）。
            *   **阶段二**：在扩展的通道空间进行第二次深度卷积滤波，再通过逐点卷积将特征投影回融合维度。
            *   这种“双深度可分离倒置瓶颈”设计显著减少了参数量，同时保持了有效的多尺度特征聚合能力。
*   **核心组件2：多任务边界引导（MTBG）策略**
    *   **设计哲学**：在不增加推理耗时和额外标注的前提下，通过对称地加强任务间在物体边界的几何一致性来减轻负迁移。
    *   **关键技术细节**：
        1.  **边界标签生成**：优先使用数据集提供的边界标注，若无，则通过对语义分割标签应用 Canny 边缘检测和膨胀操作自动生成。
        2.  **辅助边界损失**：引入一个仅在训练时存在的边界预测头，使用动态加权二元交叉熵损失强制模型学习边界模式。
        3.  **边界感知损失**：为语义分割和深度估计任务分别设计损失函数，使其**仅聚焦于边界区域**的像素进行监督，对称地精练两个任务在几何轮廓上的一致性。训练后，边界头被完全丢弃，无推理开销。

### 3. 实验设计

*   **数据集 / 场景**：
    *   **Cityscapes**：城市场景驾驶数据集，包含 2975 张训练图像，500 张验证图像。评估 19 类语义分割和深度估计（使用 CREStereo 生成的深度图，范围 [0.001, 80.0] 米）。
    *   **NYUv2**：室内场景数据集，包含 795 张训练图像，654 张测试图像。评估 40 类语义分割和深度估计（范围 [0.001, 10.0] 米）。
*   **Benchmark 与对比方法**：
    *   **评价指标**：采用语义分割（mIoU）和深度估计（RMSE）的联合性能分数 **JPS** 作为主要指标，全面评估多任务性能。
    *   **对比方法**：与广泛的 SOTA 多任务学习模型进行了比较，包括基于 CNN 的经典方法（如 PAD-Net， DenseMTL）、基于 Transformer 的先进方法（如 InvPT++， TaskPrompter）、基于状态空间模型的方法（如 MTMamba, MTMamba++）、混合专家模型（如 MLoRE）以及基于对抗训练的方法（如 SwinMTL）等。同时，论文还自行复现了一个强基线 **DPT-DINOv2-S**（DINOv2-S+标准DPT解码器）。

### 4. 资源与算力

*   **训练资源**：主要基准模型在 **NVIDIA RTX 3090 GPU** 上训练。消融实验的模型在一台装备 **四块 NVIDIA Titan RTX GPU** 的工作站上训练，以加快实验迭代。
*   **推理效率评估资源**：为了模拟资源受限的边缘部署场景，准确度评估和推理速度测量均在一台装有 **NVIDIA RTX 2080 笔记本 GPU** 的笔记本电脑上进行。
*   **训练时长**：论文未明确提及具体的训练总时长。

### 5. 实验数量与充分性

实验设计**较为充分、客观且公平**，覆盖了多个维度：

1.  **主实验（2组）**：在两个标准密集预测数据集（Cityscapes, NYUv2）上，与超过 10 种近年来的 SOTA 方法进行全面对比，具有广泛代表性。
2.  **效率分析（1组）**：专门设置实验对比各模型的参数量、理论计算量（GFLOPs）和**实际推理帧率（FPS）**，揭示了理论计算量与硬件实际表现之间的差异，论证了其方法的实用效率。
3.  **消融研究（2组）**：
    *   **架构演进**：逐步分析从标准 DPT 到 DPNeXt 各组件（IPA, DDSIF 的双深度可分离结构、倒置瓶颈）带来的性能与参数变化，清晰地说明了每个设计的贡献。
    *   **策略验证**：细致地分析了 MTBG 策略中不同对称性（只用 BAD、只用 BAS、全用 MTBG）对任务性能的影响，证实了对称边界引导对平衡提升的重要性。
4.  **公平性考量**：
    *   为最大程度公平，论文不仅引用了以往报告的最佳结果，有时还亲自复现基线模型（如 SwinMTL, DPT-DINOv2-S）进行对比。
    *   在推理时，对所有方法采用一致的评估协议（如单尺度滑动窗口、无测试时增强），确保比较的公正性。

### 6. 论文的主要结论与发现

*   **最优性能**：在 Cityscapes 和 NYUv2 两个关键基准上，**DPNeXt-B** 在语义分割和深度估计联合性能（JPS）上均**达到或超越了对比的 SOTA 方法**，尤其在 NYUv2 上以极少的可训练参数实现了效果最优。
*   **效率与精度兼得**：**DPNeXt-S** 以**显著更少的可训练参数**（相比标准 DPT 减少 78.6%），在 Cityscapes 上**超越了先前 SOTA 模型的性能**，并在笔记本电脑硬件上实现了**最快的推理速度**。
*   **架构设计的有效性**：DDSIF 块通过纯常规卷积操作实现了高效的特征融合，不仅减少了参数，还因其硬件友好性，实现了远优于理论 GFLOPs 所暗示的实际推理速度。
*   **策略的有效性**：MTBG 策略能以**零推理成本**有效缓解多任务间的负迁移问题，提升模型对几何边界的感知精度。

### 7. 优点

*   **方法创新性强**：DPNeXt 解码器设计精巧，完美地将现代高效 CNN 设计思想（深度可分离卷积、倒置瓶颈）应用于 ViT 解码器，实现了“1+1>2”的效果。
*   **实用价值高**：极度关注实际部署效率，通过理论分析与设备实测共同论证其边缘端部署的可行性，对机器人领域应用极具吸引力。
*   **任务交互处理优雅**：MTBG 策略简洁有效且无推理开销，为解决多任务学习中的负迁移问题提供了一个低成本、高收益的解决方案。
*   **可复现性强**：承诺开源代码和模型，且文中对实现细节（包括超参数、数据增强、损失函数权重）的描述非常详尽。

### 8. 不足与局限

*   **骨干网络依赖性**：性能高度依赖于冻结的 DINOv2-Reg 这一特定的强大 VFM。该设计对其它类型、规模或存在特定缺陷的 ViT/CNN 骨干网络的泛化性和鲁棒性未经充分探讨。
*   **多任务场景局限**：实验仅覆盖了语义分割和深度估计（以及 NYUv2 上的辅助表面法线）两个核心任务，未拓展到更多样化的密集预测任务（如实例分割、光流、BEV 映射）以证明其通用性。
*   **训练资源虽少但非“至简”**：尽管推理参数极少，但训练时仍需使用 RTX 3090 级显卡，且需要生成/处理伪深度标签（Cityscapes），数据准备流程略复杂。
*   **评价指标单一性**：深度评估仅使用了 RMSE，可能无法全面衡量深度图质量（例如，在不同深度的相对误差），联合指标 JPS 的设计相对简单。

（完）
