---
title: Bridging 3D Gaussians and Semantic Occupancy for Comprehensive Open-Vocabulary Scene Understanding from Unposed Images
title_zh: 桥接三维高斯与语义占用以实现无姿态图像的全方位开放词汇场景理解
authors: "Hu Zhu, Bohan Li, Xianda Guo, Yanlun Peng, Zheng Zhu, Xin Jin, Wenjun Zeng, Chang Wen Chen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01633v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 从无姿态图像实现开放词汇语义场景理解，结合高斯与语义占用场
tldr: 现有无位姿3D高斯方法在未观测区域约束弱，难以同时获取渲染几何、开放语义和占用。COVScene通过可微体积提升将语义高斯与稠密占用场耦合，在训练中施加体积正则化梯度，实现端到端统一框架。在ScanNet上，模型保持渲染质量同时提升开放语义分割和占用预测。该工作为无位姿稀疏图像的全面场景理解提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01633v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1750, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01633v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1853, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01633v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 786, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01633v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01633v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 887, \"height\": 360, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01633v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1866, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01633v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1857, \"height\": 1433, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01633v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01633v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01633v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1861, \"height\": 441, \"label\": \"Table\"}]"
motivation: 无位姿重建方法在未观测区域缺乏体积约束，难以预测可靠的语义占用。
method: COVScene通过可微体积提升耦合语义高斯与占用场，并加入占用熵正则化和几何蒸馏。
result: 在ScanNet上保持渲染质量竞争性，开放词汇分割和语义占用预测超越自监督基线。
conclusion: COVScene统一高斯与占用表示，为无位姿图像的3D开放词汇场景理解提供有效方案。
---

## 摘要
从稀疏、无姿态的图像中进行全面的三维场景理解，要求模型能够恢复可渲染的几何结构、开放词汇语义以及自由/占用三维空间，且不依赖外部相机标定。最近的前馈高斯方法改进了无姿态重建和语义渲染，但其高斯基元主要通过图像空间目标优化，在未观测区域仍缺乏充分约束。我们提出COVScene，一种无姿态语义高斯框架，通过可微体积提升将可渲染高斯基元与稠密语义占用场耦合。COVScene并非仅在评估时将高斯转换为体素，而是在训练计算图中提升预测的语义高斯，使得体积正则化能向高斯不透明度、几何和语义特征提供梯度。该框架结合了语义感知的几何变换器、多任务高斯解码、几何基础蒸馏和占用熵正则化，以在单一表征中支持新视角合成、开放词汇语义查询和语义占用预测。在ScanNet和ScanNet++上的实验表明，COVScene保持了有竞争力的渲染质量，改进了开放词汇分割，并在没有直接体素级监督的情况下实现了比自监督基线更强的语义占用预测。

## Abstract
Comprehensive 3D scene understanding from sparse, unposed images requires a model to recover renderable geometry, open-vocabulary semantics, and free/occupied 3D space without relying on external camera calibration. Recent feed-forward Gaussian methods improve pose-free reconstruction and semantic rendering, but their Gaussian primitives are mainly optimized through image-space objectives and remain weakly constrained in unobserved regions. We propose \textit{COVScene}, a pose-free semantic Gaussian framework that couples renderable Gaussian primitives with a dense semantic occupancy field through differentiable volumetric lifting. Instead of converting Gaussians to voxels only at evaluation time, COVScene lifts the predicted semantic Gaussians inside the training computation graph, so volumetric regularization provides gradients to Gaussian opacity, geometry, and semantic features. The framework combines a semantic-aware Geometry Transformer, multi-task Gaussian decoding, geometric foundation distillation, and occupancy entropy regularization to support novel view synthesis, open-vocabulary semantic querying, and semantic occupancy prediction within a single representation. Experiments on ScanNet and ScanNet++ show that COVScene maintains competitive rendering quality, improves open-vocabulary segmentation, and achieves stronger semantic occupancy prediction than the self-supervised baseline without direct voxel-level supervision.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：从稀疏、**无姿态图像**（即未知相机外参与内参）中实现**全面的3D场景理解**，要求模型同时恢复**可渲染几何**、**开放词汇语义**（支持任意文本查询的目标分割）以及**自由/占用空间**（即3D空间哪些区域是实体、哪些是空旷的），且不依赖外部运动恢复结构（SfM）或预先标定好的相机。
*   **研究动机**：现有前馈3D高斯泼溅（3DGS）方法（如LSM、AnySplat、Uni3R等）在无姿态重建和语义渲染上取得进步，但它们的高斯基元主要通过2D图像空间的渲染损失进行优化，在**未观测或弱观测区域缺乏直接的3D体积约束**，容易产生漂浮物、空洞等不合理的几何结构，且**不能输出稠密的语义占用信息**。
*   **整体含义**：论文提出**COVScene**，一个**无姿态的语义高斯框架**，通过**可微体积提升**将可渲染的高斯基元与稠密语义占用场耦合在一起。这使体积正则化能在**训练时**直接向高斯的不透明度、几何和语义特征提供梯度反馈，形成一个闭环的“高斯‑体积”联合优化，从而在单一模型中同时支持新视角合成、开放词汇语义查询和语义占用预测。

### 2. 论文提出的方法论

#### 核心思想
将传统3DGS中仅作为后处理的体素化步骤，转化为**训练图内的可微体积提升**，使占用场成为对高斯表征的显式3D几何约束。

#### 关键技术细节与流程
*   **语义感知几何变换器（Geometry Transformer）**：以预训练的VGGT为基础，初始化无姿态图像编码器，并加入额外的语义融合层。该模块通过帧内自注意力和帧间交叉注意力，从多视图图像中推断相机几何关系和3D感知特征，避免依赖外部SfM。
*   **多任务高斯解码**：基于密集预测变换器（DPT）结构，包含三个专用头：
    *   **相机头**：预测相对外参和内参。
    *   **深度头**：估计逐像素深度，结合预测相机将像素反投影为3D高斯中心。
    *   **语义高斯头**：融合变换器特征与预训练2D语义编码器的高分辨率特征，预测每个高斯基元的不透明度、旋转、尺度、颜色和**开放词汇语义特征**。
*   **可微体积提升**：在训练计算图中，将预测的语义高斯集合 G 转换为稠密体素场。
    *   **密度计算**：对每个体素中心 x，计算其局部支持集内高斯 g 的贡献密度 τ<sub>g</sub>(x) = σ<sub>g</sub> · exp(−½(x−μ<sub>g</sub>)<sup>⊤</sup>Σ<sub>g</sub><sup>−1</sup>(x−μ<sub>g</sub>))。
    *   **占用概率**：O(x) = 1 − exp(−∑τ<sub>g</sub>(x))。
    *   **语义特征**：F(x) = (∑τ<sub>g</sub>(x)f<sub>g</sub>) / (∑τ<sub>g</sub>(x) + ε)。
    *   推理时，对 O(x) > η 的体素，通过余弦相似度匹配文本嵌入，得到开放词汇语义标签。
*   **物理启发的优化目标**：
    *   **几何基础蒸馏** L<sub>distill</sub>：利用冻结的VGGT生成伪真值（相机、深度、点云），提供初始几何支架。
    *   **光度与语义监督** L<sub>photo</sub> + L<sub>sem</sub>：渲染RGB和语义特征图，分别与真值图像和CLIP特征计算L1/LPIPS损失和余弦相似度损失。
    *   **占用熵正则化** L<sub>occ reg</sub> = −(1/|V|) Σ [O(x)log(O(x)+ε) + (1−O(x))log(1−O(x)+ε)]。惩罚不明确的占用概率（接近0.5），促使每个体素更接近自由（0）或占用（1）的二元状态，有效抑制漂浮伪影。梯度通过提升操作回传至高斯参数。

### 3. 实验设计

*   **数据集**：
    *   训练：ScanNet和ScanNet++，遵循LSM的数据筛选协议，共约**1500个有效训练场景**。
    *   评估：**40个未见的ScanNet场景**，并在ScanNet++上做额外验证。
*   **基准任务与指标**：
    *   **新视角合成（NVS）**：PSNR、SSIM、LPIPS。源视图作为上下文输入，目标视图为互斥的留出视图，仅用于评估。
    *   **开放词汇分割（OVS）**：mIoU和mAcc，分别评估源视图和目标视图。
    *   **语义占用预测**：IoU和mIoU。当原生体素标注不可用时，按ISO协议构建语义占用真值。
*   **对比方法**：
    *   NVS和OVS：**AnySplat、LSM（扩展至多视图）、Feature‑3DGS，以及2D模型LSeg**。
    *   语义占用：有监督方法（**MonoScene、ISO、EmbodiedOcc**）以及自监督基线**SelfOcc**。
    *   额外设置**后处理体素化基线**（对AnySplat、Uni3R、不加提升的COVScene训练后转体素）来证明训练时体积耦合的必要性。

### 4. 资源与算力

文中**未明确说明**所使用的GPU型号、数量、训练时长或显存开销等具体算力信息。

### 5. 实验数量与充分性

*   **多维Task比较**：在**2、8、16、32视图**下，对比了COVScene与多个基线在NVS、OVS上的性能，并单独对比了语义占用预测（无3D标签设置）。
*   **充分的消融研究**：
    *   **训练时体积耦合 vs. 后处理体素化**：设计了AnySplat+sem.+post‑voxel.、Uni3R+post‑voxel.以及自身不加提升的变体，证实训练时体积反馈带来的显著占用mIoU提升。
    *   **关键组件消融**：在8视图设置下，分别移除**姿态蒸馏**、**可微提升**和**占用熵正则化**，评估对PSNR、SSIM、LPIPS和OVS mIoU的影响。结果表明每个组件都对性能和几何稳定性至关重要。
    *   **定性结果**：展示了开放词汇分割、新视角合成以及零样本占用预测的可视化对比。
*   **总体评价**：实验覆盖了多个相关任务和输入条件，对比了有/无姿态、有/无3D标签的多种基线，消融设计系统且目标明确，实验较为客观、公平且充分。

### 6. 论文的主要结论与发现

*   COVScene在**保持竞争性新视角合成质量**的同时，显著改善了**开放词汇分割**性能。
*   在**没有直接3D体素标签**的条件下，COVScene的语义占用预测大幅超越自监督基线SelfOcc（IoU从7.68→18.32，mIoU从6.93→17.78）。
*   **训练时的可微体积耦合** 是带来占用预测提升的关键，单纯在评估时进行后处理体素化效果远逊于COVScene。
*   **姿态蒸馏** 对无姿态重建的稳定性至关重要，移除后模型基本失效。
*   **占用熵正则化** 能有效抑制未观测区域的漂浮密度，使占用场更接近物理现实，并同步提升了渲染和分割指标。

### 7. 优点

*   **统一表征与多任务能力**：单一前馈模型同时支持逼真新视角合成、开放词汇语义查询和稠密3D占用预测，无需独立分支或后处理。
*   **训练时体积反馈机制**：可微体积提升将占用场从副产品提升为对高斯表征的正则化工具，解决了3DGS缺乏显式体积约束的固有问题。
*   **无需外部姿态**：从无标定图像端到端工作，去除了对SfM的依赖，更贴近实际应用（如机器人、AR）。
*   **开放词汇能力**：通过将开放词汇语义特征附着于高斯并参与体积提升，使语义占用预测也具备开放词汇查询潜力，突破闭集类别限制。

### 8. 不足与局限

*   **场景覆盖有限**：当前仅在**静态室内场景**（ScanNet/ScanNet++）上评估，未涉及室外、大规模场景或动态环境，泛化性有待验证。
*   **计算开销大**：论文承认联合优化渲染、语义对齐、几何蒸馏和体积正则化在计算上仍然昂贵，训练和推理效率有提升空间。
*   **监督信号依赖**：虽然不直接使用3D体素标签，但训练仍依赖2D语义特征和几何基础模型的蒸馏伪标签，模型的性能受限于这些预训练模型的质量和领域偏置。
*   **未来工作指向**：需扩展到动态4D场景，并研究在具身感知系统中的在线部署。

（完）
