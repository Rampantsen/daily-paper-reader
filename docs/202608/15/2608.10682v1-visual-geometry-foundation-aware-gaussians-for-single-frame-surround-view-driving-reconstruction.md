---
title: Visual Geometry Foundation-Aware Gaussians for Single-Frame Surround-View Driving Reconstruction
title_zh: 面向单帧环视驾驶重建的视觉几何基础感知高斯
authors: "Junhong Lin, Jinlong Wang, Xianda Guo, Yanlun Peng, Wei Zheng, Guoqing Liu, Hanli Wang, Tiesong Zhao, Wei Gao"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10682v1"
tags: ["query:d-gen-recon"]
score: 8.0
evidence: 基于3D高斯泼溅的前馈环视驾驶重建
tldr: 单帧环视重建因相机间重叠极小面临严重几何不稳定和渲染伪影，现有方法受限于上游特征几何容量不足。VGGD提出视觉几何基础感知的3D高斯泼溅框架，将几何建模前移并利用VGGT多视角先验token，通过双路径颈部解耦几何与外观、尺度预热稳定学习。在nuScenes单帧基准上取得最佳渲染质量并改善相对几何一致性，表明前端几何先验可有效缓解稀疏环视的几何歧义。该框架无需复杂解码器或辅助线索，为单帧环视驾驶重建提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10682v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10682v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1879, \"height\": 828, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10682v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 889, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10682v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1840, \"height\": 1903, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10682v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 658, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.10682v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1580, \"height\": 748, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10682v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 385, \"label\": \"Table\"}]"
motivation: 单帧环视重建因相机间重叠极小面临几何不稳定与伪影，现有方法受限于上游特征几何容量不足。
method: VGGD利用VGGT提供多视角几何先验token，通过双路径颈部解耦几何一致和外观感知表示，并采用尺度预热和混合像素-体积高斯解码器。
result: 在nuScenes单帧基准上，VGGD取得了最佳整体渲染质量，并提升了相对几何一致性。
conclusion: 将视觉几何基础先验前置到上游可有效缓解单帧环视重建的几何歧义，无需复杂解码器或辅助线索。
---

## 摘要
单帧环视重建由于相机间重叠极少而面临严重的几何不稳定性和渲染伪影。尽管现有方法依赖复杂解码器或辅助线索，但它们仍受限于上游特征较弱的几何能力。我们认为利用预训练的视觉几何先验可以增强上游表示，并缓解稀疏环视视图中的几何歧义。为此，我们提出了 VGGD，一个视觉几何基础感知的 3D 高斯泼溅框架，用于前馈环视驾驶重建，该框架将几何建模移至前端，并使基础先验适应驾驶相机设置。首先，VGGD 利用 VGGT 提供可迁移的多视图几何先验令牌。其次，我们引入双路径颈部以解耦几何一致性和外观感知表示，改善弱观测区域的外观补全。我们进一步应用尺度预热来稳定早期几何学习，并抑制自车位姿变化下的尺度漂移。最后，我们使用混合像素-体积高斯解码器生成可渲染的 3D 高斯场景，用于新视角合成。在 nuScenes 单帧基准上的实验表明，VGGD 在所比较的方法中取得了最佳的整体渲染质量，并提升了相对几何一致性。

## Abstract
Single-frame surround-view reconstruction faces severe geometric instability and rendering artifacts due to minimal inter-camera overlap. While existing methods rely on complex decoders or auxiliary cues, they remain bottlenecked by the weak geometric capacity of upstream features. We argue that leveraging pretrained visual geometry priors strengthens upstream representations and alleviates the geometric ambiguity in sparse surround views. To this end, we propose VGGD, a visual geometry foundation-aware 3D Gaussian Splatting framework for feed-forward surround-view driving reconstruction, which shifts geometric modeling to the frontend and adapts foundation priors to the driving camera setting. First, VGGD leverages VGGT to provide transferable multi-view geometric prior tokens. Next, we introduce a Dual-Path Neck to decouple geometry-consistent and appearance-aware representations, improving appearance completion in weakly observed regions. We further apply Scale Warmup to stabilize early geometry learning and suppress scale drift under ego-pose changes. Finally, we use a hybrid pixel--volume Gaussian decoder to produce a renderable 3D Gaussian scene for novel-view synthesis. Experiments on the nuScenes single-frame benchmark show that VGGD achieves the best overall rendering quality among the compared methods and improves relative geometric consistency.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：单帧环视驾驶重建是自动驾驶中的基础任务，用于闭环仿真、可控数据生成和高精地图构建。
- **核心问题**：单帧环视相机之间重叠区域极小，导致几何推断严重欠约束，重建结果容易出现尺度漂移、结构不一致和新视角渲染伪影。
- **现有方法的局限**：以往的前馈方法主要依赖设计复杂的解码器或辅助深度线索，但上游特征本身缺乏足够的几何能力，使得解码器难以纠正全局几何误差。
- **本文的核心观点**：应把几何建模的重点从“解码器端”前移到“上游表示端”，利用预训练的视觉几何基础模型来提供可迁移的结构先验，从而缓解稀疏环视下的几何歧义。

## 2. 论文提出的方法论

- **整体框架（VGGD）**：视觉几何基础感知的 3D 高斯泼溅框架，由几何基础前端、驾驶导向适配器和高斯解码器三部分组成。
- **几何基础前端**：
  - 使用预训练的 VGGT 模型提取多视角几何先验 token \( Z_t = \Phi(I_t) \)。
  - 保留三类特征：DINO 对齐特征、帧特征、全局特征；将帧/全局特征作为几何中心 token，并与 DINO 特征拼接，以保留语义和高频外观信息。
  - 冻结 VGGT，只训练下游模块。
- **双路径颈部（Dual-Path Neck）**：
  - 将几何一致性和外观感知表示解耦，分别通过几何路径 \( \Psi_{geo} \) 和外观路径 \( \Psi_{app} \) 预测深度 \( D_v^t \) 与外观特征 \( F_v^{app} \)。
  - 两路径特征通过逐元素相加融合，得到 \( F_v^t = F_v^{geo} + F_v^{app} \)。
  - 几何路径深度反投影得到 3D 点 \( X_v^t = \Pi^{-1}(D_v^t; K_v, T_v^t) \)，作为结构代理。
- **尺度预热（Scale Warmup）**：
  - 在前 \( S_{warm} \) 步迭代中，对几何路径预测的深度 \( D_v^t \) 施加门控 L1 损失 \( L_{warm}(s) \)，以对齐粗深度参考 \( \tilde{D}_v^t \)。
  - 预热结束后移除该直接约束，只保留输出空间的渲染深度正则，使网络能进行端到端细化。
- **混合像素–体积 3DGS 解码器**：
  - 采用两个互补的高斯头：像素头使用完整的 \( (F_t, X_t) \) 保留图像细节和远距离内容；体积头只使用掩码后的子集 \( (F_t \odot M_t, X_t \odot M_t) \) 进行有界 3D 补全。
  - 不同于 Omni-Scene，本文直接将颈部特征同时输入两个头，移除像素到体积的特征依赖。
- **训练目标**：
  \[
  \mathcal{L} = \mathcal{L}_{rgb} + \lambda_{perc}\mathcal{L}_{perc} + \lambda_{dep}\mathcal{L}_{dep} + \mathcal{L}_{vol\_rgb} + \lambda_{vol\_dep}\mathcal{L}_{vol\_dep} + \lambda_{warm}\mathcal{L}_{warm}(s)
  \]
  - 权重设置为 \( \lambda_{perc}=0.05, \lambda_{dep}=0.01, \lambda_{vol\_dep}=0.01, \lambda_{warm}=0.01 \)。
- **算法流程**：算法 1 总结了前向提取、深度/特征预测、反投影、高斯生成、渲染、损失计算和参数更新的完整训练过程。

## 3. 实验设计

- **数据集**：nuScenes 单帧自车中心环视重建基准。
  - 700 个训练场景、150 个验证场景，每场景约 20 秒、12 Hz。
  - 输入为单帧 6 个环绕相机图像；目标新视角包含两个端点帧的 12 个视角，另有 6 个中心视角用于监督与评估。
  - 总计 135,941 个训练 bin，30,080 个验证 bin。
- **评价指标**：
  - 渲染质量：PSNR、SSIM、LPIPS。
  - 几何一致性：渲染深度图与 Depth Anything V2 伪相对深度之间的 Pearson 相关系数（PCC），PCC 对全局平移和正缩放不变，适合评估相对深度一致性。
- **对比方法**：
  - 逐场景优化类：EmerNeRF、3DGS、PVG、DeformableGS。
  - 通用前馈类：AttnRend、MuRF、LGM、GS-LRM、pixelSplat、MVSplat、SCube。
  - 驾驶专用类：DrivingForward、Omni-Scene、STORM（时空参考模型）。
  - 所有基线均在相同单帧渲染协议下评估。
- **主要结果**：VGGD 取得 PSNR 24.85、SSIM 0.758、LPIPS 0.216、PCC 0.808，在全部指标上优于所有对比方法，且仅使用单帧输入即超过时空模型 STORM。

## 4. 资源与算力

- **论文未明确给出 GPU 型号、数量或具体训练时长**。
- 只提到使用 PyTorch 和 bfloat16 混合精度，优化器为 AdamW，学习率从 \(1\times10^{-4}\) 余弦退火至 \(1\times10^{-6}\)，主实验训练 100,000 次迭代；消融实验训练 10,000 次迭代。
- 因此，无法从文中得出实际算力消耗（如 GPU 小时数）。建议作者补充硬件配置以增强可复现性。

## 5. 实验数量与充分性

- **实验类型**：
  - 定量主实验：1 个数据集（nuScenes），1 张结果表（Table I），包含约 15 个基线方法。
  - 定性实验：图 3（单视角细节对比）、图 4（全环视对比）、图 5（消融定性对比）。
  - 消融实验：Table II，包括两组消融：
    - 尺度预热调度：SW1-None（无预热）、SW2-Always（一直预热）、SW3-Half（前半程预热）、Full VGGD（前 2000 步预热）。
    - 颈部与头部设计：NH1-Geo-PV（仅几何特征 + 像素–体积解码器）、NH2-Geo-GS（仅几何特征 + 直接 GS）、NH3-Geo&App-GS（几何与外观特征 + 直接 GS）、Full VGGD。
- **充分性与客观性**：
  - 消融实验在相同数据划分、分辨率、预处理和初始化下进行，控制变量较严格；但消融训练迭代为 10k，主实验为 100k，可能影响绝对数值，但相对趋势仍具参考价值。
  - 仅在 nuScenes 单帧基准上验证，未在多个数据集或不同天气/场景条件下测试，泛化性证据有限。
  - 对比方法覆盖广泛，但部分基线（如 STORM）并非严格单帧设定，作为参考更合适。
  - 整体实验设计较系统，能支撑核心贡献，但多数据集验证仍有欠缺。

## 6. 论文的主要结论与发现

- VGGD 在 nuScenes 单帧环视重建基准上取得最佳渲染质量（PSNR/SSIM/LPIPS）和最佳相对几何一致性（PCC）。
- 相比最强驾驶专用方法 Omni-Scene 提升 +0.58 PSNR、+0.022 SSIM、-0.021 LPIPS；相比 DrivingForward 提升 +0.53 PSNR、+0.026 SSIM。
- 消融实验表明：
  - 完全移除尺度预热会导致性能大幅下降（PSNR 21.92），说明早期尺度锚定对稳定几何至关重要。
  - 持续施加预热无额外收益，反而略微降低外观质量，证明瞬态预热更优。
  - 双路径颈部与像素–体积解码器的组合对渲染质量和几何一致性都有显著贡献；单一几何特征或直接 GS 均不能同时满足外观与几何需求。
- 核心结论：将几何建模前移至上游表示层，通过预训练几何基础先验和驾驶导向适配，能有效缓解单帧稀疏环视的几何歧义，优于仅依赖解码器设计的方案。

## 7. 优点

- **思路清晰**：将瓶颈从“解码器端”转移到“上游表示端”，切中现有前馈方法的痛点。
- **有效利用预训练先验**：VGGT 的多视角几何 token 为单帧低重叠场景提供结构支撑，而非简单作为辅助深度监督。
- **双路径设计合理**：几何路径与外观路径解耦，兼顾稳定 3D 支撑与高频纹理/语义补全，实验证明两者缺一不可。
- **尺度预热策略**：瞬态深度对齐既缓解了早期尺度漂移，又避免长期约束限制端到端细化，消融验证其有效性。
- **工程改动可控**：在 Omni-Scene 基础上只改动特征路由和加入预热，未大幅增加解码器复杂度，易于复现和扩展。
- **对比实验全面**：覆盖逐场景优化、通用前馈、驾驶专用和时空模型四类基线，结果具有说服力。

## 8. 不足与局限

- **未建模场景动态**：作者明确指出框架不处理运动目标，动态场景重建是未来方向。
- **数据集单一**：仅在 nuScenes 上验证，缺乏其他驾驶数据集（如 Waymo、KITTI-360）或跨域测试，泛化性未得到充分检验。
- **未提供算力信息**：缺少 GPU 型号、训练时间和显存占用等，影响可复现性和资源评估。
- **伪深度标签依赖**：深度监督使用 Metric3D-v2 和 Depth Anything V2 的预测结果，可能引入外部模型偏差，尤其对尺度预热阶段有直接影响。
- **消融训练步数不一致**：主实验 100k 迭代，消融 10k 迭代，可能使消融结论的绝对数值代表性稍弱（但趋势应可靠）。
- **VGGT 冻结**：几何前端完全冻结，可能限制对驾驶场景的进一步适配；文中通过适配器部分缓解，但未探索微调预训练层的潜在收益。
- **相对几何评估单一**：PCC 只衡量相对深度相关性，未评估绝对尺度误差或跨视角重投影误差，几何一致性的评价维度可更丰富。

（完）
