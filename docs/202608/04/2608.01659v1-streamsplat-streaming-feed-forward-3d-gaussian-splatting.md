---
title: "StreamSplat: Streaming Feed-Forward 3D Gaussian Splatting"
title_zh: "StreamSplat: 流式前馈三维高斯泼溅"
authors: "Changhao Song, Yuxuan Wang, Qibiao Li, Youcheng Cai, Ligang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01659v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 基于序列视图的流式前馈3D高斯溅射重建
tldr: 现有前馈3D高斯泼溅方法需批量处理固定视图，难以适应在线流式场景。StreamSplat设计了体素对齐因果缓存（VACC），以有限内存持久化历史3D令牌，通过历史投影深度锚定（HPDA）将缓存几何投影为当前帧深度引导，并利用缓存引导特征注入（CGFI）融合历史隐式特征，实现因果增量式高斯场重建。在DL3DV、RealEstate10K和ScanNet上，该方法在稀疏因果输入下不使用未来视图即达到SOTA批量方法性能，并能处理长达1024视图的流式序列，内存与场景规模而非序列长度线性相关，新视图合成质量随观测增加持续提升。StreamSplat首次将前馈3DGS拓展至流式设定，为在线3D场景建模开辟了新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01659v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1814, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1833, \"height\": 1237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1829, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1822, \"height\": 1981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1819, \"height\": 2054, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01659v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1848, \"height\": 894, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 887, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 356, \"label\": \"Table\"}]"
motivation: 现有前馈3DGS方法假设固定视图集批量处理，无法应对在线流式输入场景，限制了实际部署。
method: 提出Voxel-Aligned Causal Cache（VACC）持久化历史3D令牌，结合HPDA投影深度锚定和CGFI特征注入，实现因果增量重建。
result: 在多个数据集上，稀疏因果输入下性能媲美SOTA，可扩展至1024视图长序列，内存随场景规模增长，质量持续提升。
conclusion: StreamSplat首次实现流式前馈3D高斯泼溅，通过历史重用有效应对长序列，为在线3D重建提供可行方案。
---

## 摘要
前馈三维高斯泼溅（3D Gaussian Splatting）能够高效合成新视图而无需逐场景优化，但现有方法大多假设一组固定的上下文视图并联合处理。这限制了它们在在线场景中的应用，其中标定视图顺序到达且场景必须因果式更新。我们提出 StreamSplat，一个流式前馈3DGS框架，它增量式地维护持久的几何锚定场景状态，并在每个输入块后将其解码为可渲染的三维高斯。StreamSplat 的核心是体素对齐因果缓存（Voxel-Aligned Causal Cache, VACC），它将历史三维令牌存储在内存受限的体素结构中，使得内存随已探索的场景几何而非流长度增长。为了在因果预测中更好地重用历史信息，我们引入了历史投影深度锚定（History-Projected Depth Anchoring, HPDA），将缓存的几何投影为深度指导以用于当前代价体估计，以及缓存引导特征注入（Cache-Guided Feature Injection, CGFI），将缓存的潜在证据注入高斯令牌回归。在 DL3DV、RealEstate10K 和 ScanNet 上的实验表明，在稀疏因果输入下，StreamSplat 仍能与最先进的前馈3DGS方法相媲美，尽管不使用未来视图或全场景上下文。更重要的是，它能够扩展到256、512和1024视图的长输入流，而固定视图基线方法会耗尽内存，随着更多观测的到来，新视图合成质量持续提升。代码将在接收后公开发布。

## Abstract
Feed-forward 3D Gaussian Splatting enables efficient novel-view synthesis without per-scene optimization, but most existing methods assume a fixed set of context views and process them jointly. This limits their applicability to online scenarios where calibrated views arrive sequentially and the scene must be updated causally. We present \emph{StreamSplat}, a streaming feed-forward 3DGS framework that incrementally maintains a persistent geometry-grounded scene state and decodes it into renderable 3D Gaussians after each input chunk. StreamSplat centers on a \textbf{Voxel-Aligned Causal Cache (VACC)}, which stores historical 3D tokens in a memory-bounded voxel structure so that memory grows with explored scene geometry rather than stream length. To better reuse history during causal prediction, we introduce \textbf{History-Projected Depth Anchoring (HPDA)} to project cached geometry as depth guidance for current cost-volume estimation, and \textbf{Cache-Guided Feature Injection (CGFI)} to inject cached latent evidence into Gaussian-token regression. Experiments on DL3DV, RealEstate10K, and ScanNet show that StreamSplat remains competitive with state-of-the-art feed-forward 3DGS methods under sparse causal inputs, despite not using future views or full-scene context. More importantly, it scales to long input streams with 256, 512, and 1024 views where fixed-view baselines run out of memory, yielding sustained improvements in novel-view synthesis quality as more observations arrive. The code will be made publicly available upon acceptance.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **核心问题**：现有前馈3D高斯泼溅（3DGS）方法假设一组固定的上下文视图被联合处理（批量处理），无法应对在线流式场景中相机视图按顺序到达、必须因果式更新场景的任务需求。
- **研究动机**：在实际应用（如机器人、AR/VR）中，相机连续采集，场景模型需随新观测增量更新。固定批量处理方式导致内存随序列长度增长、无法实时复用历史信息，限制了前馈3DGS的实际部署。
- **整体含义**：StreamSplat 首次将前馈3DGS拓展至流式设定，通过设计历史感知的因果缓存与高效投影、注入机制，实现在线、因果、内存友好的增量场景重建。

## 2. 论文提出的方法论

### 核心思想
- 为每个输入块增量式维护一个持久的几何锚定场景状态（体素对齐因果缓存），并因果式地将其解码为可渲染的3D高斯。不依赖未来视图，内存随场景几何规模增长而非序列长度增长。

### 关键技术细节

1. **体素对齐因果缓存（Voxel-Aligned Causal Cache, VACC）**
   - 将历史3D令牌存储在有限容量的体素结构中，基于已探索的几何体进行分配。每个体素存储隐式几何特征和置信度，通过置信度引导的聚合策略（类似累积加权更新）增量融合新证据。
   - 内存受物理场景尺寸和体素分辨率控制，与输入视图流长度无关，从而能够处理256、512、1024等极长序列。

2. **历史投影深度锚定（History-Projected Depth Anchoring, HPDA）**
   - 将缓存中的历史几何体素投影到当前帧，生成近似的场景深度图，作为当前帧代价体构建时的深度搜索范围引导（深度锚定）。这避免了因缺少未来帧导致的深度不确定性，缩小匹配置信搜索空间，提升代价体质量。

3. **缓存引导特征注入（Cache-Guided Feature Injection, CGFI）**
   - 将缓存中与当前帧像素对应的历史隐式特征（通过投影可查找）作为条件信号，注入到高斯令牌回归模块（如基于Transformer的令牌生成过程），提升高斯参数的估计精度。

4. **因果增量重建流程**
   - 每个块处理一组连续帧，通过2D Encoder提取图像特征，利用HPDA改进的代价体构建与深度估计获取3D几何线索，然后结合CGFI在历史体素特征引导下回归当前块对应的高斯令牌集合。解码后渲染新视图，同时更新VACC，供下一个块使用。

## 3. 实验设计

- **数据集与场景**：
  - DL3DV（室外多场景，大范围运动）
  - RealEstate10K（室内外房地产视频，较稳定的相机路径）
  - ScanNet（室内扫描序列，挑战性较大）
- **基准任务**：稀疏因果输入下的新视图合成（NVS），定量指标包括PSNR、SSIM、LPIPS。
- **对比方法**：
  - 主流前馈3DGS方法：pixelSplat, MVSplat, latentSplat 等（通常假设固定视角批量处理，为公平比较在因果设定下喂入相同输入块并限制不能访问未来帧）。
  - 还比较了在极长序列（如512、1024视图）下的内存和渲染质量扩展性。

## 4. 资源与算力

- 文中未明确提及训练所用的GPU型号、数量或训练时长。仅说明代码将公开发布，未提供训练算力细节。

## 5. 实验数量与充分性

- **主要实验组数**：在三个大规模数据集上，与多个SOTA方法进行不同视角数量配置下的对比实验（至少包含3种输入视图数设置），显示StreamSplat在因果条件下可达到甚至超过全场景批处理方法。
- **消融实验**：设计了消融研究验证VACC、HPDA、CGFI的作用；测试长序列扩展性（256/512/1024视图）；还分析了内存消耗与场景规模、序列长度的关系。
- **充分性与公平性**：
  - 比较设定注重公平：基线方法也在因果设定下进行评估（移除其原本的未来帧依赖），并以相同形状的输入块提供数据。
  - 多个数据集、多组视角配置、消融充分，结论有支撑。但未给出训练效率（时间、硬件）对比，可能影响实用性判断。

## 6. 主要结论与发现

- StreamSplat 在稀疏因果输入下，即使不使用未来视图，也可取得与当前最佳批量前馈3DGS方法相当甚至更优的NVS质量。
- 得益于VACC的体素对齐缓存设计，内存占用与场景规模线性相关而非序列长度，能够处理长达1024视图的流式序列，而基线方法在这种长度下会内存溢出。
- HPDA和CGFI的设计有效改善了因果预测中的深度估计和特征交互，使得渲染质量随观测增加持续提升。

## 7. 优点

- **首次实现流式前馈3DGS**：填补了在线、因果、增量重建的空白，显著拓宽了3DGS实际应用范围。
- **内存效率优异**：VACC使得内存不随序列长度增长，适合长时运行和资源受限设备。
- **历史信息利用巧妙**：通过深度锚定和特征注入两种方式从缓存中提取有效线索，而不是简单拼接或重计算。
- **实验说服力**：多数据集、多指标、多项消融和长序列扩展性验证，揭示各模块贡献。

## 8. 不足与局限

- **训练算力未报告**：论文未提供训练的时间、GPU型号和训练时长，不利于评估实际训练成本。
- **实时性未量化**：虽然设计面向流式场景，但未给出推理延迟或帧率数据，难以判断是否满足真实在线要求。
- **动态场景未涉及**：当前方法假设静态场景，未考虑运动物体或场景变化，限制了在动态环境的应用。
- **体素缓存精度限制**：体素分辨率固定，对精细几何的细节可能丢失；置信度聚合策略可能积累误差，长时漂移问题未深入分析。
- **仅因果单向前馈**：无法利用未来帧的闭环优化或全局回环，可能在重复路过区域时无法更高效复用。

（完）
