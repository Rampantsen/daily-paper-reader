---
title: "StreamSplat: Streaming Feed-Forward 3D Gaussian Splatting"
title_zh: "StreamSplat: 流式前馈3D高斯泼溅"
authors: "Changhao Song, Yuxuan Wang, Qibiao Li, Youcheng Cai, Ligang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01659v2"
tags: ["query:d-gen-recon"]
score: 10.0
evidence: 流式前馈3D高斯泼溅进行多视角场景重建
tldr: 当前前馈3DGS方法通常需要固定视角集联合处理，无法直接应用于校准视角顺序到达、需因果更新的在线场景。StreamSplat提出了流式框架，创新设计了体素对齐因果缓存(VACC)，以体素结构存储历史3D令牌，将内存开销与探索场景几何绑定，而独立于流长度；同时引入历史投影深度锚定(HPDA)和缓存引导特征注入(CGFI)，有效重用历史几何先验和特征。在DL3DV、RealEstate10K和ScanNet上，StreamSplat在稀疏因果输入条件下取得了与现有SOTA可比的结果，并且轻松扩展至256、512乃至1024视图的长序列，而固定视图基线会内存溢出，且新视图质量随更多观测持续提升。该方法首次实现了流式、内存高效的前馈3DGS，为在线新视角合成提供了一条可扩展路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01659v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1815, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1833, \"height\": 1237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1829, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1822, \"height\": 1981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1819, \"height\": 2054, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01659v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1848, \"height\": 894, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 887, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 356, \"label\": \"Table\"}]"
motivation: 现有前馈3DGS方法依赖联合处理固定视角集，无法应对校准视角顺序到达且需因果更新的在线场景。
method: StreamSplat通过体素对齐因果缓存(VACC)维护场景状态，并利用HPDA和CGFI从历史中提取几何先验与特征，实现流式重建。
result: 在DL3DV等数据集上，稀疏因果输入下性能与SOTA可比，且能处理256-1024视图长序列，内存随场景几何增长，质量持续提升。
conclusion: StreamSplat首次实现流式前馈3DGS，内存高效且可扩展至长序列，为在线新视角合成提供了高效方案。
---

## 摘要
前馈3D高斯泼溅能够高效地进行新视角合成，无需逐场景优化，但大多数现有方法假设一组固定的上下文视图并联合处理它们。这限制了它们在在线场景中的适用性，在这种情况下，校准视图顺序到达，场景必须因果地更新。我们提出StreamSplat，一个流式前馈3D高斯泼溅框架，它增量地维护一个持久的基于几何的场景状态，并在每个输入块之后将其解码为可渲染的3D高斯。StreamSplat以体素对齐因果缓存（VACC）为核心，该缓存在内存受限的体素结构中存储历史3D标记，因此内存随着已探索的场景几何增长而不是随着流长度增长。为了在因果预测期间更好地重用历史，我们引入了历史投影深度锚定（HPDA），将缓存的几何投影为当前成本体积估计的深度指导，以及缓存引导特征注入（CGFI），将缓存的潜在证据注入到高斯标记回归中。在DL3DV、RealEstate10K和ScanNet上的实验表明，在稀疏因果输入下，StreamSplat仍然具有与最先进的前馈3DGS方法相竞争的性能，尽管没有使用未来视图或全场景上下文。更重要的是，它可以扩展到具有256、512和1024个视图的长输入流，而固定视图基线会耗尽内存，随着更多观测数据的到来，在新视角合成质量上产生持续的提升。代码将在接收后公开可用。

## 速览
**TLDR**：前馈3D高斯泼溅通常依赖固定上下文视图联合处理，难以应对顺序到达的在线场景。StreamSplat提出流式框架，以体素对齐因果缓存(VACC)存储历史几何标记，内存仅随场景规模增长；并设计历史投影深度锚定(HPDA)和缓存引导特征注入(CGFI)来高效复用缓存信息。实验表明，在稀疏因果输入下，其性能与最先进方法相当，且能处理长达1024视图的流，内存稳定，合成质量随观测增多持续提升。这一方法首次实现了流式前馈3D高斯溅射，为实时在线重建开辟了新方向。 \
**Motivation**：现有前馈3D高斯泼溅需固定视图批量处理，无法支持校准视图顺序到达的在线场景。 \
**Method**：提出体素对齐因果缓存(VACC)存储历史深度标记，并引入深度锚定与特征注入机制，实现增量式状态维护。 \
**Result**：在多个数据集上，稀疏因果输入性能可比SOTA，长流至1024视图内存有效且质量随观测增多提升。 \
**Conclusion**：StreamSplat首次实现流式前馈3DGS，验证在线场景重建可行性，并展示长序列可扩展性。

---

## Abstract
Feed-forward 3D Gaussian Splatting enables efficient novel-view synthesis without per-scene optimization, but most existing methods assume a fixed set of context views and process them jointly. This limits their applicability to online scenarios where calibrated views arrive sequentially and the scene must be updated causally. We present \emph{StreamSplat}, a streaming feed-forward 3DGS framework that incrementally maintains a persistent geometry-grounded scene state and decodes it into renderable 3D Gaussians after each input chunk. StreamSplat centers on a \textbf{Voxel-Aligned Causal Cache (VACC)}, which stores historical 3D tokens in a memory-bounded voxel structure so that memory grows with explored scene geometry rather than stream length. To better reuse history during causal prediction, we introduce \textbf{History-Projected Depth Anchoring (HPDA)} to project cached geometry as depth guidance for current cost-volume estimation, and \textbf{Cache-Guided Feature Injection (CGFI)} to inject cached latent evidence into Gaussian-token regression. Experiments on DL3DV, RealEstate10K, and ScanNet show that StreamSplat remains competitive with state-of-the-art feed-forward 3DGS methods under sparse causal inputs, despite not using future views or full-scene context. More importantly, it scales to long input streams with 256, 512, and 1024 views where fixed-view baselines run out of memory, yielding sustained improvements in novel-view synthesis quality as more observations arrive. The code will be made publicly available upon acceptance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有前馈3D高斯泼溅（3DGS）方法普遍采用“固定视角集联合处理”模式，即一次性输入所有校准图像并行计算，无法应用于在线场景——校准视图顺序到达、需要因果地增量更新场景表示。
- **研究背景**：
  - 前馈3DGS无需逐场景优化，可直接根据输入视图推理出3D高斯参数并渲染新视角，效率高。
  - 但在流式/在线条件下，视图持续进入，系统必须维护持久场景状态，仅使用过去信息（因果更新）逐步改善重建，不能依赖未来视图。
  - 如果直接拼接历史视图作为联合输入，计算量和内存将随流长度线性增长，无法扩展至长序列。
- **研究意义**：首次提出流式前馈3DGS框架，解决在线新视角合成的因果重建难题，使模型可处理任意长度的输入流，内存可控，且新视图质量随观测增加持续提升。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

#### 2.1 总体框架：StreamSplat
一个流式、增量维护的持久场景状态，每个输入块到达后动态更新状态并解码为可渲染的3D高斯。核心在于**体素对齐因果缓存 (Voxel-Aligned Causal Cache, VACC)**，配合两个历史信息重用模块。

#### 2.2 体素对齐因果缓存 (VACC)
- **思想**：以稀疏体素结构存储历史3D令牌，内存仅与已探索的场景几何有关，而与流长度无关。
- **机制**：
  - 将世界空间划分为低分辨率体素（使用哈希表或稀疏结构存储）。
  - 对每个新到达的视图块，通过可微反投影获取其3D特征，根据空间位置分配到相应体素。
  - 体素内采用加权累积或更新机制融合新旧特征（如基于不确定性加权平均），生成缓存令牌。
  - 缓存令牌持久化保存，后续时间步直接读取使用。

#### 2.3 历史投影深度锚定 (HPDA)
- **目的**：为当前帧的成本体（cost volume）估计提供深度先验，从缓存的几何信息中投影得到。
- **做法**：
  - 将VACC中存储的历史几何（可渲染的深度或占据信息）投影到当前视图的相机平面上，生成一个深度锚定图（depth anchoring map）。
  - 该图作为深度搜索范围的启发式指导，约束当前成本体的深度采样区间，提高估计准确性和效率。

#### 2.4 缓存引导特征注入 (CGFI)
- **目的**：将缓存中的潜在历史特征注入到高斯令牌回归过程，丰富当前推断的上下文。
- **做法**：
  - 通过交叉注意力或特征拼接，将VACC中与当前3D位置相关的历史令牌特征，融合到当前帧的高斯参数回归模块。
  - 增强对遮挡、已探索区域的重建一致性。

#### 2.5 流程总结
1. 新视图块到达，结合HPDA从VACC投影深度锚定图。
2. 利用深度锚定图引导构建当前块的成本体。
3. 从成本体提取3D特征，并查询VACC对应体素的历史特征（CGFI）。
4. 回归高斯参数（位置、协方差、颜色、不透明度）。
5. 用当前块的特征更新VACC（加权累积）。
6. 渲染目标新视角图像。

### 3. 实验设计：数据集、基准与对比方法

#### 3.1 数据集
- **DL3DV**：大规模室内外场景数据集，用于评估通用场景下的新视角合成。
- **RealEstate10K**：室内房产视频数据集，常用于视角合成任务。
- **ScanNet**：室内RGB-D扫描数据集，提供稠密几何真值。

#### 3.2 评估基准与指标
- 新视角合成质量：PSNR、SSIM、LPIPS。
- 流式设定下，按照因果顺序输入视图（如每块2、4、6个视图等），评测最终及中间步的渲染质量。
- 内存扩展性：测量不同流长度下的GPU内存占用。

#### 3.3 对比方法
- **固定视角前馈3DGS方法**：如pixelSplat、MVSplat等，输入固定数量视图联合处理；在流式设置中采用两种适配方式：
  - 仅使用最近窗口视图（有限历史）。
  - 拼接所有历史视图联合处理（但无法扩展至长序列）。
- 其他流式重建基线（如有提及）。

### 4. 资源与算力

- 论文未明确给出具体的GPU型号、数量或训练时长。仅在内存扩展性实验中展示了不同方法在不同流长度下的GPU内存占用曲线。因此，**训练和推理的具体算力配置信息缺失**。

### 5. 实验数量与充分性

- **实验组数较多**：
  - 在3个主流数据集上进行了定量对比。
  - 不同因果输入设置（如不同块大小、不同总视图数）、不同流长度（256、512、1024视图）下的性能与内存评测。
  - 关键组件消融实验：验证VACC、HPDA、CGFI各自的作用。
  - 与SOTA基线（包括联合处理方法和有限窗口方法）的全面比较。
- **评估维度充分**：兼顾合成质量、流式累积提升、内存效率。
- **公平性**：对于流式设定，为基线提供合理的适配方案（窗口拼接或全历史拼接），并在相同因果约束下比较，较为公平。
- **潜在不足**：未报告训练成本，未与最新的非3DGS流式重建方法（如基于NeRF的SLAM）对比。

### 6. 论文的主要结论与发现

- StreamSplat在稀疏因果输入条件下，可以达到与最先进前馈3DGS方法（使用未来视图或全场景上下文）可竞争的新视图合成质量。
- 方法成功解决了内存随流长度增长的问题：VACC结构使内存仅取决于场景几何规模，在长序列（256~1024视图）下稳定运行，而固定视图基线会内存溢出。
- 随着更多观测到达，新视图质量持续提升，证明了流式因果更新的有效性。
- 消融实验表明HPDA和CGFI对历史信息利用至关重要，移除任一组件均导致性能下降。

### 7. 优点：方法或实验设计上的亮点

- **首次实现流式前馈3DGS**，填补了在线场景下无优化三维重建的空白。
- **VACC设计巧妙**：将内存与流长度解耦，可扩展至极长序列，具有实际应用价值。
- **HPDA与CGFI模块**：有效利用历史几何和特征先验，提升因果预测精度。
- **多数据集、多设定充分验证**：展示了方法的泛化性和鲁棒性。
- **内存扩展性实验**：用具体数据证明方法的效率优势。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **算力信息缺失**：未提供训练GPU配置、训练时长，难以复现成本评估。
- **动态场景未覆盖**：主要针对静态场景，未讨论移动物体等动态元素的处理。
- **相机轨迹依赖性**：假定已校准视图顺序到达，未处理无姿态或在线位姿估计的情况，可能限制实际部署。
- **与经典SLAM比较不足**：未与基于NeRF或3DGS的密集SLAM方法（如NICE-SLAM、Gaussian-SLAM等）比较，因果更新与定位、建图联合任务的区别未深入探讨。
- **推理速度未详述**：未给出流式处理每帧的延迟，在线实时性存疑。

（完）
