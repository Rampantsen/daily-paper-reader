---
title: 3D Consistency Optimization for Self-Supervised Monocular Video Depth Estimation
title_zh: 自监督单目视频深度估计的三维一致性优化
authors: "Yuanye Liu, Ke Zhang, Junzhe Jiang, Li Zhang, Vishal Patel, Xiahai Zhuang"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15681v1"
tags: ["query:d-gen-recon"]
score: 8.0
evidence: 将单目视频深度估计转化为无约束多视角三维重建
tldr: 单目视频深度估计是内窥镜导航中3D推理与具身AI的关键，但现有自监督方法往往逐帧独立处理或仅依赖弱时序正则，缺乏底层3D场景整体感知，导致几何不一致及跨帧漂移。本文提出将顺序视频深度估计重塑为无约束多视图3D重建问题，充分挖掘3D基础模型几何先验，设计由图像光度渲染、显式世界坐标几何对齐和多尺度时间梯度一致性驱动的3D一致性优化框架。该框架将孤立帧锚定到全局一致3D结构，在自监督训练和零样本临床测试中均达到最优空间精度，显著优于帧/视频深度估计器和多视图重建基线。为内窥镜导航等具身AI提供几何一致性深度感知。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15681v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1222, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15681v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15681v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1244, \"height\": 1004, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15681v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1244, \"height\": 1004, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15681v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1245, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15681v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 309, \"label\": \"Table\"}]"
motivation: 现有视频深度估计方法缺乏3D场景整体感知，导致几何不一致与跨帧漂移，亟需全局一致性优化。
method: 将顺序视频深度估计转化为无约束多视图3D重建，结合光度渲染、世界坐标对齐及多尺度时间梯度一致性约束进行优化。
result: 在自监督和零样本临床场景中，方法达最优空间精度，超越基于帧、视频深度估计和多视图重建基线。
conclusion: 利用3D基础模型先验实现跨帧一致深度估计，为内窥镜导航等下游3D推理和具身AI奠定基础。
---

## 摘要
可靠的单目视频深度估计对下游的三维推理和内窥镜导航中的具身人工智能至关重要。然而，现有的自监督方法通常独立处理视频帧，或者依赖弱时间正则化。这些方法缺乏对底层3D场景的整体感知，不可避免地导致几何不一致的预测和严重的跨帧漂移。为了解决这些局限，我们引入了一种新范式，将序列视频深度估计重新构想为无约束的多视图三维重建问题，从而能够充分利用近期三维基础模型中嵌入的强大几何先验。我们方法的核心是一个由三个约束驱动的三维一致性优化框架：图像级光度渲染、显式世界坐标几何对齐，以及多尺度时序梯度一致性。这种统一的优化巧妙地将孤立帧锚定到全局一致的3D结构上。我们的方法已在自监督训练场景和具有挑战性的零样本临床环境中得到验证。结果表明，所提出的方法达到了最先进的空间精度，优于基于帧、基于视频的深度估计器和多视图三维重建基线。

## Abstract
Reliable monocular video depth estimation is crucial for downstream 3D reasoning and embodied AI in endoscopic navigation. However, existing self-supervised approaches typically treat video frames independently or rely on weak temporal regularization. These methods, lacking a holistic perception of the underlying 3D scene, inevitably suffer from geometrically inconsistent predictions and severe cross-frame drift. To address these limitations, we introduce a new paradigm that recasts sequential video depth estimation as an unconstrained multi-view 3D reconstruction problem, enabling full exploitation of the powerful geometric priors embedded in recent 3D foundation models. The core of our approach is a 3D consistency optimization framework driven by three constraints: image-level photometric rendering, explicit world-coordinate geometric alignment, and multi-scale temporal gradient consistency. Such unified optimization elegantly anchors isolated frames to a globally coherent 3D structure. Our method has been validated in both the self-supervised training scenarios and challenging zero-shot clinical environments. Results show that the proposed approach achieves state-of-the-art spatial accuracy, outperforming the frame-based, video-based depth estimators and the multi-view 3D reconstruction baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究问题**：内窥镜手术视频中，由于硬件和物理约束（单目、无可靠位姿跟踪），如何实现无位姿（pose-free）且自监督的单目深度估计，并确保深度预测在几何上一致、时序上稳定，从而支持三维推理和具身智能（如重建、导航、机器人辅助）。
- **背景与动机**：现有自监督方法大多将视频视为独立帧或仅施加弱的时序正则，缺少对底层三维场景的整体感知，导致小误差累积为严重的跨帧漂移和闪烁，破坏后续三维重建的可靠性。因此，需要一种能从全局三维一致性出发进行深度估计的新范式。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将单目视频深度估计转化为无约束的多视图三维重建问题，利用三维基础模型的强大几何先验，通过**三维高斯溅射（3D Gaussian Splatting）** 表示场景，并联合优化密集深度图和相机位姿，使所有帧锚定到统一的世界坐标系中。**仅在训练时进行三维一致性优化，推理时单次前馈即可预测深度，无需测试时优化**。
- **技术框架与主要步骤**：
  - **场景表示**：用一组三维高斯原语 `P = {pi}` 表示手术场景，每个原语含中心、透明度、协方差、球谐系数等。
  - **几何理解网络**：基于多视图Transformer（如Alternating-Attention Transformer），输入视频帧经DINOv2特征提取后，通过帧内和全局注意力编码时空一致性，输出：
    - 相机头：预测每帧外参 `T_t` 和统一内参 `K`（固定第一帧位姿为单位矩阵以消除规范歧义）。
    - 深度头：预测初始像素级深度 `D_t^init`，用于反投影生成高斯中心。
    - 高斯头：预测其余高斯属性（透明度、朝向、球谐系数）。
  - **可微分渲染**：通过高斯溅射渲染出预估RGB `Î_t` 和深度 `D̂_t`。
  - **三维一致性优化损失**（仅用于训练）：
    1. **图像级一致性 `L_rgb`**：渲染RGB与输入帧的L1损失。
    2. **世界坐标几何一致性 `L_3D`**：将像素反投影到世界空间，再投影到相邻视图，计算深度一致性（Huber损失），并设计有效像素掩码处理遮挡和离群值。
    3. **时序梯度一致性 `L_temp`**：在多尺度下约束相邻帧深度图的梯度差异，抑制闪烁并保持几何边缘。
  - 总损失：`L = λ1 L_rgb + λ2 L_3D + λ3 L_temp`，所有权重均设为1。

### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集**：
  - **SCARED**：da Vinci Xi系统采集的35段高分辨率内窥镜视频，含投影仪测量的真值深度、位姿和内参；划分24段训练、3段验证、8段测试。
  - **Hamlyn**：真实体内内窥镜和腹腔镜视频（来自[15]的校正版本，使用Libelas生成真值深度），全部用于零样本评测。
- **评价指标**：Abs Rel, Sq Rel, RMSE, RMSE log, `δ<1.25`（δ1），预测深度与真值经中值缩放对齐后评价。
- **对比方法**：EndoDAC (2024), VDA (2025), EndoDAV (2025), AnySplat (2025)。

### 4. 资源与算力
- **明确提及**：使用单张NVIDIA A100 GPU进行训练，优化器为AdamW，训练20,000次迭代，预热2,000次，峰值学习率`2×10^{-4}`，骨干参数乘0.1倍学习率。每迭代采样16帧视频剪辑。

### 5. 实验数量与充分性
- **主要实验数量**：
  - 两个公开数据集上的整体对比（SCARED、Hamlyn），各包含定量和定性结果。
  - 消融实验：在SCARED上分别移除或组合三个一致性约束（表格2），验证各组件的贡献。
  - 骨干网络泛化性实验：将一致性优化应用到VDA和AnySplat两个不同骨干上（表格3）。
- **充分性与公平性**：实验覆盖了帧式、视频式深度估计和多视图重建基线，数据量适中；消融和跨骨干实验设计合理，能清晰证明各组件的有效性，客观公平。

### 6. 论文的主要结论与发现
- 所提出的三维一致性优化框架能显著减少几何漂移和时序不稳定，在SCARED和Hamlyn上所有指标均超越现有最强方法。
- 世界坐标对齐损失是消除跨帧漂移的关键，多尺度时序梯度损失有效抑制闪烁，光度损失保障外观重建。
- 该框架可泛化到不同的深度估计骨干，带来一致的性能提升，证明其在通用性上的优势。

### 7. 优点：方法或实验设计上的亮点
- **范式创新**：将视频深度估计重构为位姿无关的多视图三维重建，巧妙利用三维基础模型先验。
- **联合优化**：三维高斯表示、相机位姿与深度图的统一优化，无需真值位姿，自洽且端到端。
- **训练-推理解耦**：训练时进行全局一致性优化，推理时仅需单次前馈，无测试时优化开销。
- **损失设计**：三维空间显式几何对齐与多尺度时序梯度约束，有效解决漂移和闪烁问题。
- **实验完备**：在两个临床相关数据集、多种基线上完成定量与定性验证，并给出消融和跨架构分析。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **数据集局限**：仅在两个内窥镜数据集上评测，缺少更广泛的手术场景或自然场景视频验证。
- **真值生成问题**：Hamlyn数据集的真值由Libelas生成，可能引入误差，影响评估可靠性。
- **训练固定相机内参**：假设全局统一内参，而实际内窥镜变焦情况未考虑，可能限制通用性。
- **动态场景处理**：虽用Huber损失缓解动态组织影响，但未进行显式运动分割，复杂形变和遮挡下仍可能错误。
- **计算资源与速度**：虽单A100训练，但Transformer骨干和高斯溅射计算开销较大，未讨论推理延迟，实际部署待验证。
- **长期漂移**：虽减少了跨帧漂移，但在超长序列或剧烈运动中是否完全保持全局一致性未深入探讨。

（完）
