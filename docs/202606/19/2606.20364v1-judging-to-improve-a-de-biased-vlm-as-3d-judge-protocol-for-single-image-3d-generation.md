---
title: "Judging to Improve: A De-biased VLM-as-3D-Judge Protocol for Single-Image 3D Generation"
title_zh: 以评判促改进：一种面向单图像三维生成的去偏 VLM 作为三维评判器协议
authors: "Ali Asaria, Tony Salomone, Deep Gandhi"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20364v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 利用VLM评判器微调单图像到三维生成器，直接提升从图像生成三维物体的质量
tldr: 单图像3D生成中，廉价几何与CLIP代理评估不可靠，提出无偏VLM-as-3D评判器协议进行质量排序。本文进一步将评判器硬化用于优化：分离训练与评估评判器、校正位置偏差、修复三种失败模式，并将其用于微调TRELLIS。轻量参数高效适应仅能匹配强基线，最高胜率0.50，未能超越；条件修复是关键。协议可重复使用，表明纯公开数据上轻量微调不足以突破上限。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20364v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1704, \"height\": 571, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20364v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1529, \"height\": 268, \"label\": \"Table\"}]"
motivation: 使VLM评判器从排序走向无标签优化，以廉价方式将强生成器TRELLIS专用于家具类，但需解决优化暴露的失败模式。
method: 硬化评判器：采用独立Qwen2.5-VL-7B与InternVL3-8B打破循环，校正位置偏差，并修复图像过载、几何隐藏渲染与无参考错误奖励，用于指导条件修复等轻量适应。
result: 多种轻量微调方法均未超过TRELLIS基线，胜率最高仅0.50，条件修复是唯一移动几何的位点；独立基样本无可学偏好。
conclusion: 仅凭公开数据与轻量PEFT能匹配但难超越强基线；评判协议可推广用于未来研究，超越需更深度方法。
---

## 摘要
一项伴随研究建立了一种去偏、跨模型的视觉语言模型作为三维评判器，可在廉价几何与CLIP代理表现不佳时，可靠地对单图像到三维网格质量进行排序。本文探讨：该评判器的偏好能否以低成本且无需人工标注的方式，将强大的开源生成器 TRELLIS 专门化于单一资产类别（家具）？将评判器从排序引入优化，正是本文的核心工作。将VLM评判器推入训练与评估循环会暴露排序中未曾触发的失效模式，因此我们的贡献在于对评判器进行优化级别的加固：训练评判器（Qwen2.5-VL-7B）与评估评判器（InternVL3-8B）保持分离，以打破循环性；校正位置偏差；并针对三种失效模式（图像过载、隐藏几何的溅射渲染、以及奖励清洁但错误输出的无参考评判）进行修复，并提供校准证据（明显差距胜率为0.83-1.0；基础模型对比约为0.5）。利用该协议作为独立评估器，并仅使用公开模型与数据，通过轻量级参数高效适配，我们发现所提方法能达到强大基线的水平，但未超越。独立的基础样本几乎不携带可学习的偏好（顺序翻转率0.94），因此信号必须通过质量对比构造来设计。在六种适配方法、两种输入设置和一系列退化程度中，最具针对性的方法——在严重退化下的条件器修复——达到了与基础模型持平的水平（0.50），而没有任何方法能超过≥65%的胜率目标。这一结果是机制性的：干净输入使评判器饱和，flow-DIT微调通过采样器被冲刷掉，而条件器修复才是改变几何的关键所在。在n=8个物体的测试中，胜率具有方向性指示。以廉价的适配匹配强大的公开数据基线，这本身就提供了信息：超越它需要的不仅仅是基于公开数据的轻量级PEFT，且该评判协议具有可复用性。

## Abstract
A companion study established a de-biased, cross-model VLM-as-3D-judge that reliably ranks single-image-to-3D mesh quality where cheap geometry and CLIP proxies fall short. This paper asks: can that judge's preferences specialize a strong open generator, TRELLIS, on one asset class (furniture), cheaply and without human labels? Taking the judge from ranking to optimization is where the work lives. Pushing a VLM judge into the training and evaluation loop exposes failure modes ranking never triggered, so our contribution is an optimization-grade hardening of the judge: a training judge (Qwen2.5-VL-7B) held distinct from an evaluation judge (InternVL3-8B) to break circularity; position-bias correction; and fixes for three failure modes (image overload, geometry-hiding splat renders, and reference-free judging that rewards clean-but-wrong outputs), with calibration evidence (clear-gap win-rate 0.83-1.0; base-vs-base ~0.5). Using this protocol as an independent evaluator, and working only from public models and data with lightweight parameter-efficient adaptation, we find our methods match the strong base rather than exceed it. Independent base samples carry essentially no learnable preference (0.94 order-flip rate), so signal must be engineered by quality-contrastive construction. Across six adaptation methods, two input regimes, and a severity sweep, the most targeted - conditioner repair under severe degradation - reaches parity (0.50) with the base, while no method clears the >=65% win-rate target. The result is mechanistic: clean inputs saturate the judge, flow-DIT fine-tuning washes out through the sampler, and conditioning repair is the locus that moves geometry. Win-rates are directional at n=8 objects. Matching a strong public-data base with cheap adaptation is itself informative: exceeding it needs more than lightweight PEFT on public data, and the judge protocol is reusable.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Judging to Improve: A De-biased VLM-as-3D-Judge Protocol for Single-Image 3D Generation》的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：单图像到3D生成领域已有强大模型，一个自然的研究方向是将这些模型在特定资产类别（如家具）上进行**低成本、无标签的特化**，以超越原始模型。
*   **核心问题**：一篇伴随研究证实了经过设计的视觉语言模型评判器（VLM-as-3D-Judge）比廉价的几何或CLIP代理更可靠。本文循此思路，提出一个更激进的问题：**能否直接利用这个评判器的偏好作为训练信号，去优化3D生成器？**
*   **整体含义**：这项工作并非要提供一个超越一切的3D生成模型，而是将 VLM 评判器从“评估排序”推向“闭环优化”的过程中所遇到的挑战、解决方案和最终发现的局限性作为核心贡献。最终结果表明，**在仅使用公开数据和轻量级参数高效微调的条件下，适配模型只能达到强大基线的同等水平，而无法超越。**

### 2. 方法论：优化级去偏 VLM-3D-评判器协议

*   **核心思想**：为了将评判器可靠地用于模型训练，必须解决优化反馈循环中暴露的、而单纯排序任务不会触发的失效模式。论文的核心贡献是提出了一套 **“优化级加固协议”**。
*   **关键技术细节**：
    *   **跨模型评判独立**：为打破优化中的循环论证，协议强制分离**训练评判器** (Qwen2.5-VL-7B) 和**评估评判器** (InternVL3-8B)，所有最终胜率由独立的评估评判器报告。
    *   **位置偏差校正**：对每个生成对，评判器会在`(A, B)`和`(B, A)`两种顺序下进行评判，**仅保留两次判断一致的裁决**。不一致（即翻转）的配对视为存在位置偏差并被丢弃，这同时起到了置信度过滤的作用。
    *   **三个修复的失效模式**：
        1.  **图像过载**：向评判器展示参考图+多视角面板（共7张图）会导致其仅根据位置回答（翻转率100%）。**修复方法**：采用两张图的单一对比模式。
        2.  **高斯泼溅渲染隐藏几何**：泼溅渲染会使破碎网格看起来干净，导致评判器无法识别几何缺陷。**修复方法**：改用 **网格法线贴图的2×2蒙太奇视图**，使几何破绽暴露无遗。
        3.  **无参考评判奖励错误输出**：在没有输入参考图的情况下，评判器会奖励“干净但错误”的输出。**修复与缓解**：在法线蒙太奇覆盖度的基础上，通过“明显差距”校准来确认评判器在显著质量差异下的可靠性。

### 3. 实验设计

*   **数据集/场景**：使用公开的 `3D-FUTURE` 家具数据集。
*   **基础模型与适配**：选定强大的单图像3D生成器 `TRELLIS` 作为基线。仅使用轻量级参数高效微调，主要有两类：
    1.  **Flow-DIT 微调**：在其可训练的潜在结构Transformer上应用`LoRA`。
    2.  **条件器修复适配器**：冻结基础模型的Flow-DIT，仅训练一个适配器修复输入的`DINOv2`特征。
*   **比较方法与基准**：
    *   **六种适配方法**：对Flow-DIT的`SFT-on-best`、`DPO (β=0.1, 0.5)`、`ORPO`，以及`SFT-on-clean`（在退化输入下）；独立的`条件器修复适配器`。
    *   **两种输入场景**：
        1.  **干净场景**：分布内的3D-FUTURE高质量渲染图。
        2.  **困难退化场景**：对输入图像施加合成的裁剪、遮挡、下采样和模糊，并在不同严重程度（轻度/中度/重度）上进行扫描测试。
    *   **对比基准**：与未经适配的原始TRELLIS基线进行胜率比较。成功目标为评估评判器胜率 **≥ 65%**。
    *   **训练信号构建**：由于基线的独立采样几乎无质量差异，研究者通过**质量对比构造**（高计算量“HI”生成 vs. 低计算量退化生成）来人为制造可学习的偏好信号。

### 4. 资源与算力

*   **算力提及**：论文指出该研究在 **Lambda 云计算平台**上的 **H100 级别 GPU (主要是 H100-SXM5)** 上运行。
*   **缺失信息**：**未提供**具体的 GPU 使用数量、模型训练总时长或总 GPU 小时数等详细信息。

### 5. 实验数量与充分性

*   **实验矩阵**：实验覆盖了 **6种适配方法 × 2种输入场景 × 3种退化严重程度** 的组合，形成了较为全面的实验矩阵。
*   **评估样本量**：最终胜率评估在 **8个按物体ID严格隔离的测试对象** 上进行。论文承认这是一个**小样本量，结果具有“方向性”而非精确性**，并谨慎避免提供置信区间。
*   **客观性与公平性**：评估方案颇为严密。
    *   **训练/评估分离**：强制执行训练评判器与评估评判器的分离，避免过拟合评判器偏好。
    *   **校准证据**：通过“明显差距对照”（预期高胜率）和“基线对比对照”（预期胜率≈0.5）验证了评判器的无偏性和分辨能力，确保了零胜率是真实结果而非评判器失效。
    *   **机制分析**：通过监测几何有效性变化、适配器特征重建误差等指标，对实验结果进行了机制的拆解，而不仅仅是给出最终分数。

### 6. 主要结论与发现

*   **无超越，仅持平**：在给定的实验条件下，**没有任何一种适配方法能超越强大的TRELLIS基线**。最高的胜率是重度退化下的条件器修复，达到了与基线持平的 **0.50胜率**，未能达到 ≥ 65% 的目标。
*   **机制被分离**：论文将“无法超越”的原因归结为三个具体的、局部的机制：
    1.  **饱和度**：在干净、分布内的数据上，基线质量已极高，评判器对微小差异无偏好，优化无增益空间。
    2.  **采样器冲刷**：对Flow-DIT的微调，其微小梯度在长链的流采样过程中被“冲刷”殆尽，导致最终生成网格的几何无变化。
    3.  **条件器是瓶颈**：条件器特征修复是**唯一能移动输出几何的干预点**，其效果随退化程度单调递增，但上限也仅仅是修复信息损失，达到基线水平，无力创造超越基线的质量。

### 7. 优点

*   **问题定义深刻**：将工作重点从“得到一个更好的模型”转向“理解为什么在这种设定下无法得到一个更好的模型”，并构建了坚固的评判协议。
*   **方法论严谨**：对VLM评判器在优化场景下的失效模式进行了细致的诊断（图像过载、渲染欺骗性、无参考偏差）并提出了有效的修复方案，该协议具有高可复用性。
*   **实验设计周密**：通过分离评判器、设立多重校准对照、分析机制，确保了结论（即使是“负面的”持平结论）的**高度可信**。
*   **细致的因果分析**：没有停留在结果表面，而是通过机制分离技术，指出了饱和度、采样器冲刷和条件器瓶颈这三个具体失败原因。

### 8. 不足与局限

*   **评估样本有限**：最终胜率仅基于 8 个独立测试物体，结论仅具方向性。对于条件修复器的1.0胜率（n=2）到0.50胜率（n=8）的崩溃，论文自身也承认了噪声问题。
*   **适配方式受限**：所有实验仅限轻量级参数高效微调（LoRA或小型适配器），未探索全量微调、更大容量适配器或更长训练周期可能带来的不同结果。
*   **场景泛化性存疑**：实验限于单一的TRELLIS模型、单一的家具类别和**合成的退化类型**（非真实世界的模糊、遮挡等），结论可能无法直接推广到其他基础模型、资产类别或真实世界的失真。
*   **评判器本身的局限**：“质量”的定义依赖于VLM评判器家族，文中明确指出未进行大规模人类偏好研究验证，其“可靠性”指代的是跨模型一致性和对显著质量差距的敏感性，而非与人类判断的对齐。

（完）
