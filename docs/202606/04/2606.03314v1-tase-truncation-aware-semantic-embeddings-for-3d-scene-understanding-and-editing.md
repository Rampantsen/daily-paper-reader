---
title: "TASE: Truncation-Aware Semantic Embeddings for 3D Scene Understanding and Editing"
title_zh: "TASE: 面向三维场景理解与编辑的截断感知语义嵌入"
authors: "Tim-Felix Faasch, Jochen Kall, Lucas Nunes, Jens Behley, Cyrill Stachniss"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03314v1"
tags: ["query:d-gen-recon"]
score: 9.0
evidence: 通过截断感知嵌入实现语义3D场景理解与编辑
tldr: 高保真语义3D场景表示对机器人、自动驾驶和仿真至关重要，但现有方法提供有限的可控编辑能力。TASE将预训练2D语义特征投影到截断感知嵌入空间，通过通道截断实现从精细到抽象的层级化表示，并利用尺度-平移等变性损失提升多视图一致性。该方法支持文本驱动的3D场景编辑，显式控制编辑与原始内容的关联度，允许更大幅度的几何修改；微调扩散模型减轻几何变化伪影。实验表明TASE在场景编辑任务上显著优于先前方法，尤其在大几何修改编辑中表现突出，为3D场景的可控编辑开辟了新路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1249, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1211, \"height\": 1232, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03314v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1259, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03314v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1251, \"height\": 204, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03314v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 952, \"height\": 414, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03314v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1111, \"height\": 404, \"label\": \"Table\"}]"
motivation: 高保真3D场景表示需要灵活编辑能力，但现有方法在控制编辑强度和几何修改方面存在不足。
method: TASE将预训练2D语义特征投影到截断感知嵌入空间，通过通道截断实现分层语义表示，结合尺度-平移等变性损失加强多视图一致性，并微调扩散模型消除几何伪影。
result: TASE在3D场景编辑中显著优于先前方法，尤其在大幅几何修改编辑中性能突出。
conclusion: TASE通过截断感知嵌入实现了层级化语义控制，为3D场景编辑提供了灵活、精细且能处理大幅几何修改的新方案。
---

## 摘要
高保真语义三维场景表示对于机器人、自动驾驶和仿真等众多应用至关重要。此外，编辑此类表示的能力使开发者能更轻松地将这些应用适配到特定的目标场景。当前方法对可控编辑的支持有限。我们提出TASE，一种将预训练的二维语义特征投影到截断感知嵌入空间中以实现灵活三维场景编辑的方法。我们的方法显式优化了一个特征空间，其中逐步减少特征通道会产生越来越抽象的语义表示，而保留更多通道则保留细粒度细节。此外，我们利用尺度和平移等变损失提升特征的多视图一致性。由此得到的截断感知嵌入空间支持对三维场景进行文本驱动的编辑，提供对编辑遵循原始场景内容程度的显式控制，并允许比先前方法更大幅度的修改。此外，我们为编辑扩散模型提出了一个微调阶段，以减轻几何变化引起的伪影。实验结果展示了在三维场景编辑中的竞争性能，在涉及大规模几何修改的编辑上显著优于先前方法。

## Abstract
High-fidelity semantic 3D scene representations are crucial for numerous applications, including robotics, autonomous driving, and simulation. Beyond this, the ability to edit such representations enables developers to adapt these applications more easily to specific target scenarios. Current approaches provide limited support for controllable editing. We introduce TASE, a method that projects pretrained 2D semantic features into a truncation-aware embedding space to enable flexible 3D scene editing. Our method explicitly optimizes a feature space in which progressively reducing feature channels yields increasingly abstract semantic representations, while retaining more channels preserves fine-grained detail. Additionally, we improve multi-view consistency of the features using a scale- and translation-equivariance loss. The resulting truncation-aware embedding space enables text-driven edits to 3D scenes, providing explicit control over how strongly edits adhere to the original scene content and allowing more substantial modifications than prior methods. Moreover, we propose a finetuning stage for the editing diffusion model to mitigate artifacts caused by geometric changes. Experimental results demonstrate competitive performance in 3D scene editing, substantially outperforming prior methods on edits involving large geometric modifications.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义
- **研究背景**：高保真语义3D场景表示（如3D高斯泼溅，3DGS）在机器人、自动驾驶和仿真中日益重要，但现有方法缺乏对场景进行结构化、可控编辑的能力。
- **核心问题**：当前3D编辑方法依赖于离散类别逻辑或固定的2D特征，难以实现多视图一致性、可控的语义抽象层级，且难以应对大幅几何修改（例如将自行车变为摩托车）。
- **整体含义**：提出**TASE**（截断感知语义嵌入），将预训练2D语义特征投影到一个通道有序、无位置偏见的嵌入空间，通过通道截断控制从精细到抽象的语义表示，并利用扩散模型实现文本驱动的、允许大幅几何变化的3D场景编辑。

## 方法论
- **截断感知语义嵌入（TASE）的构建**：
  - 使用预训练DINOv3作为特征提取骨干（`MTASE_base`），获取原始特征图`F_o`。
  - 训练一个对称自编码器（`MTASE_enc`/`MTASE_dec`），将`F_o`压缩至低维`F_h`（通道数`N_h=64`）。
  - 引入**Matryoshka表示学习**：训练时随机对`F_h`的后缀通道进行零掩码（截断层级集`T={2^1,...,2^6}`），迫使早期通道捕获更重要的语义，实现可控抽象。
  - 利用**尺度-平移等变损失**`L_eqv`：随机裁剪输入图像，强制裁剪区域的特征与直接从完整图像对应区域编码得到的特征一致，消除2D绝对位置偏置。
  - 总损失：`L_TASE = ˜L_r + λ_eqv * L_eqv`，其中`˜L_r`为重建损失（余弦距离+MSE）。
- **嵌入提升至3DGS**：
  - 为每个高斯球添加随机初始化的特征向量`f_i`，与外观参数联合优化。
  - 渲染特征图的损失`L_f`为`F_rendered_h`与`F_image_h`的重建误差，并引入局部平滑损失`L_s`（k近邻高斯特征一致性），以及马氏距离正则化`L_maha`（约束特征不偏离训练统计分布）。
  - 总损失：`L_splat = λ_l1*L_l1 + λ_SSIM*L_SSIM + λ_f*L_f + λ_s*L_s + λ_maha*L_maha`。
- **3D分割**：基于用户点击提取锚点特征，用余弦相似度与空间一致性传播为高斯分配二值标签，可利用通道截断控制抽象程度。
- **编辑扩散模型训练**：
  - 基于FLUX.1[dev]的ControlNet，控制分支以TASE特征`F_h`为输入。
  - 训练时也对`F_h`进行通道截断，使模型适应不同语义层级。
  - **微调策略（Diffix3D+启发）**：构建干净/损坏图像三元组（使用AnySplat生成场景，随机扰动几何参数），模拟几何变化导致的渲染伪影，将损坏图视为中间扩散步的噪声图，微调MDiT和MCtrl，提升模型对几何编辑伪影的鲁棒性。
- **3D场景编辑流程**：
  - 全局编辑：从训练视角采样，生成编辑视图，优化高斯参数使渲染图与生成图一致，迭代中采用逐渐增多的通道保留量和噪声添加计划。
  - 局部编辑：先分割目标高斯子集，构建目标中心相机位姿，用渲染掩码进行图像修复生成，额外加入正则项与权值衰减以移除旧几何、避免过饱和。

## 实验设计
- **数据集**：使用多个未具名的公开3D场景数据集（文中提及进行多样化编辑，但未列出具体场景名称，可能为常见NeRF/3DGS评估场景）。图像级训练使用**ImageNet-1k**。
- **基准对比**：与**Direct Gaussian Editing (DGE)** 和**GaussianEditor (GE)** 等SOTA 3DGS编辑方法进行定性与定量比较。
- **评估指标**：
  - **CLIP方向相似度**（CLIP dir. sim.）：衡量编辑前后变化与文本指令的对齐度。
  - **用户调研**：评估几何修改和外观修改的偏好百分比。
  - 消融实验中还使用**PSNR、SSIM、特征损失`L_f`**评估重建质量。

## 资源与算力
- 文中未明确提及GPU型号、数量或训练时长。
- 训练过程分为：自编码器+ControlNet在ImageNet-1k上训练1个epoch（学习率1e-4/1e-5）；Diffix3D+微调使用50k图像对训练4个epoch（学习率1e-6/1e-7）。这些层级不涉及大规模3D数据，算力需求可能相对可控，但具体硬件配置未知。

## 实验数量与充分性
- **实验组数**：
  - 局部编辑的定性对比（Fig.3）。
  - 全局编辑的定性结果（Fig.4）。
  - 定量对比表（Tab.1）：CLIP dir. sim. + 用户调研。
  - 通道截断控制的定性分析（Fig.5）。
  - 消融实验（Tab.2与Fig.6）：对比PCA vs. 自编码器、有无等变损失、有无Diffix3D+微调，共6种配置。
- **充分性**：实验覆盖了核心创新点验证（通道控制性、等变性、微调），对比了当前主流方法，并进行了用户调研，具有较好的说服力。但缺少在多个标准3D场景数据集上的系统定量结果（如不同场景类型的编辑成功率），且未提及视角一致性等专项指标。

## 主要结论与发现
- TASE成功将2D语义特征转为多视图一致、通道可截断的表示，有效支持3D语义分割和编辑。
- 通过通道截断可平滑控制编辑的抽象层级，在低维时更忠实于文本指令，高维时更保持场景布局。
- 与SOTA方法相比，TASE在大幅几何编辑任务上优势显著，CLIP方向相似度大幅领先（0.1014 vs. 0.0574以下），用户偏好也明显占优。
- 等变损失对消除2D位置偏置、提升多视图一致性至关重要；Diffix3D+微调能修复几何编辑带来的伪影，恢复细节。

## 优点
- **创新性**：首次将Matryoshka表示学习与等变约束结合，实现可控语义抽象并用于3D编辑，思想新颖且有效。
- **技术完整性**：从特征学习、3D集成、分割到扩散模型训练和编辑优化，形成完整流水线。
- **显式控制**：用户通过通道截断直接调整编辑与源内容的关联度，比此前方法更灵活。
- **编辑能力突破**：能处理大几何变化（如自行车变摩托车），而现有方法多停留在颜色或纹理修改。

## 不足与局限
- **超参数敏感**：编辑过程中的截断通道数、噪声添加、学习率等需要调度，可能对场景敏感，文中未提供通用指导。
- **计算未明确**：未说明训练及编辑的GPU成本，可复现性受影响。
- **场景评估有限**：缺乏在多标准场景数据集（如Mip-NeRF360、T&T、LLFF）上的系统定量对比，消融实验也仅限于少数示例编辑。
- **语义与外观未解耦**：导致编辑时可能出现过度饱和或颜色偏差，需进一步解耦。
- **局部编辑依赖于分割准确性**，分割错误可能影响编辑质量，文中依赖简单相似度传播，对复杂场景稳健性未知。

（完）
