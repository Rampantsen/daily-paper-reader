---
title: "Distill, Diffuse, and Semanticize (DDS): Annotation-Free 3D Scene Understanding Based on Multi-Granularity Distillation and Graph-Diffusion-Based Segmentation"
title_zh: 蒸馏、扩散与语义化（DDS）：基于多粒度蒸馏和图扩散分割的无标注3D场景理解
authors: "Yijing Wang, Ruonan Li, Qilin Wang, Rongqiang Zhao, Jie Liu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08293v2"
tags: ["query:d-gen-recon"]
score: 10.0
evidence: 免标注的3D语义场景理解，直接对应3D场景语义分割需求
tldr: "在自动驾驶、数字孪生等领域，3D点云语义理解至关重要，但点级标注成本高昂，现有结构导向无标注方法虽轻量却通常产生类别不可知区域，语义辨别能力弱。DDS框架保持轻量超点结构，通过多粒度蒸馏从投影特征和分割掩码中提取视觉语义，并利用图扩散直接在3D空间传播语义，实现连贯的语义区域分割。实验表明，在真实场景下，DDS相比代表性结构导向方法，整体准确率等指标提升高达5.9%，平均准确率提升8.1%，mIoU提升2.4%。该方法有效平衡了计算效率与语义识别，为无标注3D场景理解提供了可扩展、可解释的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08293v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1867, \"height\": 734, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 1297, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1850, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1866, \"height\": 2213, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1868, \"height\": 2239, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.08293v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 870, \"height\": 472, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08293v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 776, \"height\": 470, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08293v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 919, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08293v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 775, \"height\": 462, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.08293v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 719, \"height\": 354, \"label\": \"Table\"}]"
motivation: 现有无标注3D场景理解方法难以同时兼顾语义精度与计算效率，结构导向方法缺乏语义识别，开放词汇方法开销过大。
method: DDS通过多粒度蒸馏从2D视觉特征引导3D骨干，并利用图扩散在超点上传播语义，最后通过聚类关联赋予语义标签。
result: "在真实数据集上，DDS相比结构导向基线，oAcc提升5.9%，mAcc提升8.1%，mIoU提升2.4%，达到最优性能。"
conclusion: DDS实现了轻量高效的无标注3D语义理解，增强了区域一致性和可解释性，具有可扩展性。
---

## 摘要
3D语义场景理解对于数字孪生、自动驾驶、智慧农业和具身感知至关重要，然而点云的逐点密集标注成本高昂且难以扩展。现有的无标注方法通常在语义识别和结构效率之间进行权衡：开放词汇和基础模型驱动的方法提供强大的语义先验，但往往带来大量的计算开销，而基于超点、聚类和图推理的结构导向方法虽然轻量，但通常产生类别无关的区域。我们提出了DDS，一个资源高效的结构导向框架，用于区域一致且语义化的无标注3D场景理解。DDS保留了轻量级的超点组织范式，同时从投影特征和分割派生的掩码中融入视觉语义线索。它首先执行多粒度蒸馏，在点、掩码原型和原型间三个层次上指导3D骨干网络，然后在超点上应用图扩散，直接在3D中传播语义信息，生成连贯的区域表示，无需昂贵的谱分解或密集的开放词汇3D特征场。最后，DDS使用分割-聚类关联为类别无关的3D聚类分配可解释的语义名称。在真实世界数据集上的实验表明，DDS在代表性的结构导向无标注基线中取得了最佳性能，分别在oAcc、mAcc和mIoU上提高了多达5.9%、8.1%和2.4%。这些结果证明，DDS提高了区域一致性和轻量级语义识别能力，为无标注3D场景理解提供了一个可扩展且可解释的解决方案。

## Abstract
3D semantic scene understanding is essential for digital twins, autonomous driving, smart agriculture, and embodied perception, yet dense point-wise annotation for point clouds remains expensive and difficult to scale. Existing annotation-free methods often face a trade-off between semantic recognition and structural efficiency: open-vocabulary and foundation-model-driven methods provide strong semantic priors, but often come with substantial computational costs, while structure-oriented methods based on superpoints, clustering, and graph reasoning are lightweight but often produce category-agnostic regions. We propose DDS, a resource-efficient structure-oriented framework for region-consistent and semanticized annotation-free 3D scene understanding. DDS preserves the lightweight superpoint-based organization paradigm while incorporating visual semantic cues from projected features and segmentation-derived masks. It first performs multi-granularity distillation to guide the 3D backbone at the point, mask-prototype, and inter-prototype levels, then applies graph diffusion over superpoints to propagate semantic information directly in 3D, producing coherent region representations without costly spectral decomposition or dense open-vocabulary 3D feature fields. Finally, DDS uses segmentation-cluster association to assign interpretable semantic names to category-agnostic 3D clusters. Experiments on real-world datasets show that DDS achieves the best performance among representative structure-oriented annotation-free baselines, improving oAcc, mAcc, and mIoU by up to 5.9%, 8.1%, and 2.4%, respectively. These results demonstrate that DDS improves region consistency and lightweight semantic recognition, providing a scalable and interpretable solution for annotation-free 3D scene understanding.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：3D 点云语义场景理解在自动驾驶、数字孪生等领域至关重要，但逐点人工标注成本极高，难以大规模扩展。现有无标注方法存在两难：开放词汇/基础模型驱动的方法虽语义识别能力强，但算力开销大；结构导向方法（超点、聚类、图推理）轻量，却通常只输出类别未知的区域，缺乏可解释的语义标签。
- **整体含义**：该论文试图在“轻量结构”与“语义辨识”之间取得平衡，提出一个资源高效、结果可解释的无标注 3D 场景理解框架 DDS，旨在不依赖任何人工点云标注的情况下，生成带有语义名称的连贯区域分割。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

DDS 包含两个训练阶段和一个语义推理步骤，核心思想是“蒸馏‑扩散‑语义化”。

- **阶段一：多粒度蒸馏**
  - 利用冻结的 DINOv2 提取多视图 2D 特征，通过相机投影生成 3D 教师特征 \( t_i \)（按可见视图平均）。
  - 利用冻结的 SAM3 生成 2D 掩码并投影到 3D，形成实例感知的掩码组 \( \mathcal{M} = \{M_m\} \)。
  - 对每个有效掩码计算学生原型 \( \bar{p}^{(m)}_s \) 和教师原型 \( \bar{p}^{(m)}_t \)（mask 内点特征平均）。
  - 蒸馏损失由三项组成：点级余弦对齐 \( L_{point} \)、掩码级原型对齐 \( L_{proto} \)、原型间对比学习 \( L_{nce} \)（用温度参数 \( \tau \) 计算交叉熵，正样本为同一 mask 的教师原型）。
  - 总损失 \( L_{distill} = \lambda_{point} L_{point} + \lambda_{proto} L_{proto} + \lambda_{nce} L_{nce} \)。训练后骨干网络 fθ 可直接从点云提取具有语义一致性的特征。

- **阶段二：基于图扩散的分割**
  - 使用 VCCS+区域生长将点云过分割为超点 \( S_n \)，将点特征做平均池化得到初始超点特征矩阵 \( H^{(0)} = F \)。
  - 构建超点图，用 RBF 核定义亲和矩阵 \( A_{ij} = \exp(-\gamma\|h_i - h_j\|^2_2) \)，归一化邻接矩阵 \( \tilde{A} \)，拉普拉斯矩阵 \( L = I - \tilde{A} \)。
  - 图扩散迭代：\( H^{(t+1)} = (1-\alpha)F + \alpha \tilde{A} H^{(t)} \)，其闭式解为 \( H^* = (I + \beta L)^{-1}F \)，\( \beta = \alpha/(1-\alpha) \)。此过程以 \( O(T N_s^2 C) \) 的复杂度替代昂贵的谱分解，实现语义信息在超点间的高效传播。
  - 扩散特征经方差选择、KMeans 粗分、PCA 嵌入、二次 KMeans 后生成伪标签 \( \hat{Y} \)，用于自训练分割网络。

- **语义推理：分割‑聚类关联**
  - 分割模型输出类别无关的聚类 \( \hat{C}_k \)。
  - 对测试场景，使用 SAM3 在多视图上生成掩码并投影回 3D，得到候选语义标签集合 \( L_i \)。
  - 统计每个聚类内各语义标签的投票比率 \( r_{k,q} \)，取最高且超过阈值 \( \eta \) 的标签作为该聚类的语义名称，再传播到点级，最终输出带有语义的 3D 分割结果。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：
  - nuScenes：1000 个场景，32 线 LiDAR，16 个语义类别。
  - SemanticKITTI：序列 08 作为验证，64 线 LiDAR，19 个评估类别，不使用目标域蒸馏，直接迁移 nuScenes 上的蒸馏权重。
- **评价指标**：总体准确率 (oAcc)、平均准确率 (mAcc)、平均交并比 (mIoU)。
- **对比方法**：
  - 结构导向无标注基线（主要对比对象）：K‑means、IIC、PiCIE、GrowSP、U3DS3、P‑SLCR、PointDC、PointDC‑DINOv2、LogoSP。
  - 资源密集型 open‑vocabulary 方法（仅作参考）：CLIP2Scene、OpenScene、AFOV+SAN。

### 4. 资源与算力

- **训练平台**：单张 NVIDIA RTX 3090 GPU（24 GB 显存）。
- **训练耗时**：在 nuScenes 上蒸馏阶段约 4 小时，分割阶段约 0.5 小时。
- **对比参考**：表中列出 CLIP2Scene 需 2×A100 /~40 小时，OpenScene 需 4×A100 /~24 小时，AFOV 需 4×V100 / 共 80 小时 等，凸显 DDS 的计算高效。

### 5. 实验数量与充分性

- **两个数据集的主实验**：nuScenes 和 SemanticKITTI，给出定量对比表格。
- **消融实验** (在 nuScenes 上)：
  - 组件消融：考察蒸馏策略（随机初始化、点级蒸馏、多粒度蒸馏）与超点特征处理方式（PCA、图傅里叶变换 GFT、图扩散）的组合，共 9 种设置。
  - 损失权重消融：固定 \( \lambda_{point}=1 \)，改变 \( \lambda_{proto} \) 和 \( \lambda_{nce} \)，共 8 组实验。
- **可视化分析**：错误图对比、局部案例放大、BEV 语义布局、各类 IoU 箱线图等，定性展示一致性改进。
- **充分性评价**：实验覆盖了主要结构导向基线，通过组件消融清晰证明了多粒度蒸馏和图扩散各自的收益，对比方法选取公平，提供了资源占用参考，整体较为充分。

### 6. 论文的主要结论与发现

- DDS 在结构导向无标注方法中实现了最优性能，在 nuScenes 上达到 60.7% oAcc / 31.2% mAcc / 21.2% mIoU，在 SemanticKITTI 上达到 56.2% oAcc / 29.2% mAcc / 17.7% mIoU，分别超越最强基线若干点。
- 多粒度蒸馏（点、掩码原型、原型间对比）相比单纯点级蒸馏显著提升区域一致性；图扩散在超点上的语义传播优于 PCA 和 GFT，且避免了高成本的特征分解。
- 语义推理模块能在不增加训练成本的情况下，为聚类区域赋予可解释的语义标签，在全天候、多天气条件下仍保持合理的 BEV 语义布局。
- 整体框架在保持轻量级的前提下，成功弥合了结构划分与语义辨识之间的鸿沟。

### 7. 优点：方法或实验设计上有哪些亮点

- **结构‑语义平衡**：以超点图扩散为核心，既保持结构导向方法的轻量性，又引入视觉基础模型的语义先验，实现带语义的区域分割。
- **多层次蒸馏设计**：通过点级、掩码原型级和原型间对比三个层次的约束，同时促进特征对齐、区域紧凑性和类间可分离性。
- **高效图扩散**：将超点特征传播建模为迭代扩散过程，复杂度仅为 \( O(T N_s^2 C) \)，避免传统谱方法所需的 \( O(N_s^3) \) 特征分解，计算友好。
- **可解释的语义命名**：利用测试时多视图 SAM3 掩码与 3D 聚类进行多数投票，使得原本无类别的区域获得语义标签，增强了输出的直接可用性。
- **资源开销透明对比**：主动提供与高资源 open‑vocabulary 方法的算力对比，突出自身作为“资源高效”方案的定位。
- **跨数据集泛化验证**：将在 nuScenes 上蒸馏的模型直接用于 SemanticKITTI（不同 LiDAR 配置），仍取得领先结果，表明学到的结构化语义先验具有可迁移性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **仅与结构导向方法比较**：虽然提到 open‑vocabulary 方法，但并未将其作为直接对比对象，无法体现 DDS 在语义粒度和识别种类上的上限。
- **语义命名依赖固定提示集**：SAM3 的提示需要根据数据集场景预先设定，对不同领域或开放式场景的零样本泛化能力未做深入探讨。
- **动态物体处理未明确**：尽管 BEV 可视化展示了对行人和车辆的组织能力，但缺少针对动态、快速移动物体的专项评估。
- **超点过分割的敏感性**：超点构建依赖于几何先验（VCCS+区域生长），在极度稀疏或非刚性点云上的鲁棒性尚未验证。
- **评估指标局限**：主要使用整体和类平均 IoU，但个别语义类别（如小物体、交界地带）的详细性能仅通过箱线图定性展示，未给出逐类对比数据。
- **SAM3 视图覆盖依赖性**：语义推理假设多视图 SAM3 掩码足够丰富，若相机不足或出现大面积遮挡，可能会导致语义投票噪声增加，影响命名准确性。

（完）
