---
title: "DelowlightSplat: Feed-Forward Gaussian Splatting for Lowlight 3D Scene Reconstruction"
title_zh: "DelowlightSplat: 针对低光三维场景重建的前馈高斯泼溅"
authors: "Fuzhen Jiang, Zengtian Xie, Zhuoran Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26629v1"
tags: ["query:d-gen-recon"]
score: 10.0
evidence: 利用前馈高斯泼溅从多视图图像进行低光照3D重建
tldr: 在低光环境下，前馈3D高斯重建因噪声、颜色偏移及对应困难而精度骤降，难以用于新视图合成。DelowlightSplat提出低光感知前馈框架：构建可控多视图低成本基准仅退化上下文视图，设计轻量低光适配器进行残差增强以提升可匹配性，并利用成本体推断直接预测干净的3D高斯表示。实验表明，该方法在低光新视图渲染中显著超越先前前馈方法和两阶段增强流程。该工作为低光场景的实时3D重建提供了有效的前馈解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26629v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1499, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1506, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 606, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26629v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 745, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有前馈高斯重建方法在低光条件下因噪声、颜色偏移和匹配误差而失败，无法为机器人和AR/VR提供可靠重建。
method: 构建可控低光基准仅退化上下文视图，设计轻量低光适配器残差增强特征，结合成本体推断直接预测干净3D高斯。
result: 在低光基准上，DelowlightSplat显著优于先前前馈方法和两阶段增强流程，实现干净新视图渲染。
conclusion: DelowlightSplat通过低光适配器和成本体推断有效解决了低光退化问题，推动了前馈高斯重建在低光环境中的应用。
---

## 摘要
从稀疏带姿态图像进行新视角合成与三维重建是机器人与AR/VR的核心。然而，由于噪声、色偏和不可靠的对应关系，前馈三维高斯重建在低光条件下会失效。我们提出DelowlightSplat，一个面向低光环境的前馈高斯泼溅框架，用于生成干净的新视角渲染。我们通过仅降低上下文视图的质量，同时保持目标视图干净，构建了一个可控的多视角低光基准。我们引入一个轻量级的低光适配器进行残差增强以改善可匹配性，并将其与基于成本体的多视角推断相结合，直接预测干净的三维高斯。实验表明，在低光条件下，DelowlightSplat显著优于以往的前馈方法和两阶段流程。

## Abstract
Novel-view synthesis and 3D reconstruction from sparse posed images are central to robotics and AR/VR. Yet, feed-forward 3D Gaussian reconstruction fails under lowlight due to noise, color shifts, and unreliable correspondence. We propose DelowlightSplat, a lowlight-aware feed-forward Gaussian splatting framework for clean novel-view rendering. We build a controllable multi-view lowlight benchmark by degrading only context views while keeping target views clean. We introduce a lightweight Lowlight Adapter for residual enhancement to improve matchability, and couple it with cost-volume-based multi-view inference to directly predict clean 3D Gaussians. Experiments show that DelowlightSplat significantly outperforms previous feed-forward method and two-stage pipeline under lowlight conditions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在低光环境下，现有的前馈三维高斯泼溅（Feed-Forward 3DGS）重建方法由于图像噪声、色偏以及跨视角对应关系不可靠，导致几何结构错误、外观过度平滑，难以实现高质量的新视角合成。
- **研究动机**：现实应用中（如机器人巡检、低照度 AR/VR）常常面临低光照拍摄条件，但前馈重建模型大多在正常曝光数据上训练，直接应用会失效。
- **整体含义**：该工作旨在赋予前馈 3DGS 重建网络低光感知能力，使其能够从低光照输入的稀疏视角中直接重建出光照正常、干净的三维场景，为低光条件下的实时三维重建提供一种端到端的解决方案。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：不将低光增强与三维重建解耦为两个独立阶段，而是在前馈高斯重建网络中嵌入一个**轻量低光适配器**，通过残差学习改善跨视角特征的可匹配性，然后利用成本体（Cost-Volume）多视角推断直接预测干净的三维高斯场景。
- **可控多视角低光基准构建**
  - 仅对**上下文视图**进行概率性退化（Gamma 暗化、曝光缩放、RGB 通道偏移、高斯模糊），**目标视图**保持清洁，从而提供干净监督信号。
- **低光适配器（Lowlight Adapter）**
  - 输入：低光照上下文图像 \(I_i\)。
  - 输出：适配后图像 \(\tilde{I}_i = \text{clip}(I_i + \lambda \cdot \tanh(\Delta(I_i)))\)。
  - \(\Delta(\cdot)\) 由少量卷积残差块实现，\(\lambda\) 控制残差幅度。
  - 适配器端到端训练，优化目标并非单独“增强”图像，而是提升后续多视角匹配的可靠性。
- **前馈高斯泼溅网络**
  - **多视角特征编码**：从适配后的视图提取 2D 特征，通过成本体聚合跨视角几何一致性证据。
  - **深度驱动的几何构建**：预测深度与相关密度量，反投影得到高斯中心 \(\mu(u,d) = o + d \cdot r(u; K, T)\)；协方差由预测的尺度与旋转参数化；不透明度经压缩到 (0,1)。
  - **外观建模**：使用球谐系数表示视角相关颜色。
  - **损失函数**：联合使用像素损失（\(\ell_2\) 或 Charbonnier）与感知损失，反向传播更新整个前馈网络。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法
- **数据集**：基于大规模带姿态视图数据集（RealEstate10K 类，论文引用 [11]）构建，仅对上下文视图施加低光退化，目标视图保持干净，以此构成训练与评估基准。
- **对比基准**：
  1. **Lowlight MVSplat**：直接将低光视图送入 MVSplat [3]，不进行任何增强。
  2. **DarkIR → MVSplat**：先用暗光恢复模型 DarkIR [16] 逐图像增强，再将增强结果送入 MVSplat，代表“先恢复再重建”的两阶段管线。
  3. **DelowlightSplat（本文方法）**：轻量适配后进行多视角推断，直接输出干净的三维高斯场景。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点
- 文中明确说明：所有实验在 **单块 NVIDIA RTX 4090 GPU** 上运行，批次大小为 **8**，训练 **10,000 步**。
- 未提及具体训练耗时，但基于网络结构的轻量化设计及迭代步数，可推测训练成本相对较低。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- **主实验**：在低光基准上对上述三种方法进行定量（PSNR, SSIM, LPIPS）与定性（可视化结果）比较，展示新视角合成的清晰度和一致性。
- **消融实验**：针对四种孤立退化因素（Gamma 暗化、曝光缩放、RGB 通道偏移、模糊）进行逐项叠加实验，评估每种因素对重建的影响及方法在各退化层次下的表现。
- **实验充分性评价**：
  - 实验设计公平，基准退化操作可控、可复现，对比方法具有代表性（直接重建、两阶段增强）。
  - 消融实验清晰展示了不同低光效应的破坏性以及方法的缓解能力，但仅在单一数据集的合成低光退化下进行，未涉及真实物理低光采集数据，泛化性验证稍显不足。
  - 总体而言，实验具有一定说服力，但覆盖面（如更多场景类型、真实低光视频）可进一步扩展。

### 6. 论文的主要结论与发现
- 低光退化会严重破坏基于成本体的前馈高斯重建，直接使用低光视图或先逐图增强再重建均存在几何模糊、色偏不一致等问题。
- 通过在重建流水线前嵌入低光适配器进行残差增强，可有效改善跨视角可匹配性，使成本体推断更稳定。
- DelowlightSplat 端到端联合优化适配与三维重建，显著优于直接重建（Lowlight MVSplat）和两阶段增强（DarkIR → MVSplat），在 PSNR、SSIM、LPIPS 上均有大幅提升，输出的新视角图像清晰、一致。

### 7. 优点：方法或实验设计上有哪些亮点
- **端到端低光感知重建**：将低光增强与三维重建深度融合，避免了两阶段管线中的多视图不一致问题。
- **残差式轻量适配器**：设计简单，计算开销低，易于训练，同时避免过度增强导致的伪影。
- **可控退化基准**：通过组合可配置的低光操作，能够系统性地研究各退化因素对三维重建的影响，并为后续研究提供可复现的测试平台。
- **清晰的消融分析**：直观展示了不同低光因素如何破坏匹配和几何推断，并验证了所提适配器的有效性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **合成退化与真实低光存在差距**：实验仅在仿真退化（Gamma、曝光、色彩偏移、高斯模糊）上进行，未涵盖真实低光场景中的复杂噪声模型（如读出噪声、光子散粒噪声）和动态范围问题。
- **仅支持两视图输入**：当前流水线限定为两幅上下文视图，无法处理更多视角或更长时域序列，限制了在稠密多视图或视频输入场景下的应用。
- **相机内参与目标视图要求**：依赖已知相机标定，且目标视图需为正常曝光，实际应用中获取干净视角可能存在困难。
- **数据集单一**：仅在单一大规模场景数据集上构建低光基准，未在更多样的室内/室外、不同尺度场景上验证泛化性。
- **缺少运行时效率分析**：虽然方法为前馈式，但未报告推理速度或内存占用，难以评估实时应用潜力。

（完）
