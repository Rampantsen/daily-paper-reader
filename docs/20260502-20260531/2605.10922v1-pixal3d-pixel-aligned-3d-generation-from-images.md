---
title: "Pixal3D: Pixel-Aligned 3D Generation from Images"
title_zh: "Pixal3D: 从图像生成像素对齐的3D"
authors: "Dong-Yang Li, Wang Zhao, Yuxin Chen, Wenbo Hu, Meng-Hao Guo, Fang-Lue Zhang, Ying Shan, Shi-Min Hu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10922v1"
tags: ["query:d-gen-recon"]
score: 10.0
evidence: 基于像素对齐的图像到3D物体生成方法，确保高保真度
tldr: 尽管近期3D生成模型在几何分辨率和外观真实感上取得进步，但从图像生成3D资产的像素级忠实度仍是关键瓶颈，问题源于多数模型在规范空间合成形状并通过注意力注入图像特征，导致像素与3D对应关系不明确。Pixal3D借鉴3D重建思想，提出像素对齐的3D生成范式，直接从输入图像视角生成，并设计像素反投影调节机制，显式地将多尺度图像特征提升为3D特征体积，建立直接且无歧义的像素-3D对应。该方法不仅生成高质量3D资产，且忠实度接近重建水平；还可通过聚合多视角特征体积扩展至多视图生成，并应用于场景合成，生成物体分离的高保真3D场景。Pixal3D首次展示了可扩展的3D原生像素对齐生成，为从单图或多视图创建高保真物体或场景提供了新颖的路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10922v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1854, \"height\": 1008, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1755, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1757, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1753, \"height\": 2256, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1701, \"height\": 2218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 851, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 879, \"height\": 295, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.10922v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1814, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10922v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 808, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10922v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 244, \"label\": \"Table\"}]"
motivation: 现有图像到3D生成模型主要瓶颈在于像素级忠实度不足，根本原因是规范空间合成与注意力机制引入的隐式2D-3D对应问题。
method: Pixal3D提出像素对齐生成范式及像素反投影调节，显式构建多尺度特征到3D特征体积的映射，直接从输入视角生成3D资产。
result: Pixal3D生成资产忠实度近似重建水平，并可扩展至多视图聚合和场景合成，产生物体分离的高保真3D场景。
conclusion: Pixal3D首次展示可扩展的3D原生像素对齐生成，解决对应模糊，为单图或多视图高保真对象与场景生成提供新路径。
---

## 摘要
近期3D生成模型的进展已迅速提升图像到3D合成的质量，实现了更高分辨率的几何和更逼真的外观。然而，衡量生成3D资产与输入图像像素级忠实度的保真度仍是一个核心瓶颈。我们认为这源于一个隐式的2D-3D对应问题：大多数3D原生生成器在规范空间中合成形状，并通过注意力注入图像线索，使得像素到3D的关联模糊不清。为解决此问题，我们从3D重建中汲取灵感，提出了Pixal3D，一种像素对齐的3D生成范式，用于从图像创建高保真3D资产。Pixal3D并非在规范姿态下生成，而是以像素对齐的方式直接生成3D，与输入视图保持一致。为此，我们引入了一种像素反投影条件机制，将多尺度图像特征显式提升到3D特征体中，建立直接的像素到3D对应，消除了歧义。我们表明，Pixal3D不仅可扩展，能产生高质量3D资产，还大幅提升了保真度，接近重建的保真水平。此外，Pixal3D自然地扩展到多视图生成，通过聚合各视图的反投影特征体。最后，我们展示了像素对齐生成有利于场景合成，并提出了一个模块化管线，从图像生成高保真、物体分离的3D场景。Pixal3D首次大规模展示了3D原生的像素对齐生成，并为从单幅或多视图图像生成高保真物体或场景提供了新的启发途径。

## Abstract
Recent advances in 3D generative models have rapidly improved image-to-3D synthesis quality, enabling higher-resolution geometry and more realistic appearance. Yet fidelity, which measures pixel-level faithfulness of the generated 3D asset to the input image, still remains a central bottleneck. We argue this stems from an implicit 2D-3D correspondence issue: most 3D-native generators synthesize shape in canonical space and inject image cues via attention, leaving pixel-to-3D associations ambiguous. To tackle this issue, we draw inspiration from 3D reconstruction and propose Pixal3D, a pixel-aligned 3D generation paradigm for high-fidelity 3D asset creation from images. Instead of generating in a canonical pose, Pixal3D directly generates 3D in a pixel-aligned way, consistent with the input view. To enable this, we introduce a pixel back-projection conditioning scheme that explicitly lifts multi-scale image features into a 3D feature volume, establishing direct pixel-to-3D correspondence without ambiguity. We show that Pixal3D is not only scalable and capable of producing high-quality 3D assets, but also substantially improves fidelity, approaching the fidelity level of reconstruction. Furthermore, Pixal3D naturally extends to multi-view generation by aggregating back-projected feature volumes across views. Finally, we show pixel-aligned generation benefits scene synthesis, and present a modular pipeline that produces high-fidelity, object-separated 3D scenes from images. Pixal3D for the first time demonstrates 3D-native pixel-aligned generation at scale, and provides a new inspiring way towards high-fidelity 3D generation of object or scene from single or multi-view images. Project page: https://ldyang694.github.io/projects/pixal3d/

---

## 论文详细总结（自动生成）

好的，以下是对论文《Pixal3D: Pixel-Aligned 3D Generation from Images》的结构化、深入的中文总结。

### 1. 论文的核心问题与整体含义
*   **核心问题**：当前主流的图像到3D生成方法虽然能产出高质量的几何与外观，但在**保真度**（即生成的3D资产与输入图像像素级的忠实匹配程度）上存在显著瓶颈。
*   **问题根源**：作者将该瓶颈归因于**隐式的2D-3D对应问题**。现有方法通常在规范化空间合成物体，并通过跨注意力机制注入图像特征，导致2D像素与3D位置之间的对应关系模糊且不明确。
*   **整体含义与研究动机**：作者借鉴3D重建领域中“显式对应”的思想，提出了一种名为**Pixal3D**的像素对齐3D生成范式。其基本思想是将3D生成从“规范化空间”转移到“像素对齐的视角空间”，通过建立无歧义的像素到3D映射，从根本上解决保真度不足的问题，使生成模型同时兼具重建的精确性和生成的创造性。

### 2. 论文提出的方法论
*   **核心思想**：**像素对齐生成**。不同于传统方法在规范姿态下合成物体，Pixal3D直接在输入图像的相机坐标系下生成3D模型，使物体的朝向与输入视角一致。
*   **关键技术细节**：
    *   **像素反投影条件机制**：这是Pixal3D实现像素对齐的核心。
        1.  使用DINOv2提取输入图像的2D特征图。
        2.  将目标3D空间（一个单位立方体）内的每一个体素，根据相机投影关系，反投影回2D图像平面上。
        3.  通过双线性采样获取该像素位置的特征，并将其赋值给该体素。
        4.  重复此过程，为整个立方体填充特征，形成一个与3D噪声体素空间完全对齐的**3D特征体积**。
        5.  最后，将此3D特征体积与扩散模型中的3D噪声数据直接相加，作为显式的空间条件信号。
    *   **多尺度特征融合**：为了解决DINOv2特征粒度较粗、丢失细节的问题，引入了一个特征上采样器，将低分辨率的patch特征图上采样到原图分辨率，得到高分辨率的细节特征图。在反投影时，同时采样并融合多尺度特征，以此保留和传递更精细的图像信息。
    *   **模型架构流程**：
        1.  **结构化潜在表示学习**：使用一个变分自编码器（VAE）将像素对齐的稀疏有符号距离场（SDF）压缩为高效的稀疏潜在向量。
        2.  **图像条件处理**：通过上述反投影机制，将输入图像转化为多尺度的3D特征体积和全局语义token。
        3.  **两阶段生成**：首先，一个密集的扩散Transformer（DiT）基于条件信号生成粗粒度的物体结构（占位网格）；然后，一个稀疏DiT在此基础上进一步生成精细的稀疏潜在向量。
        4.  **网格解码**：最终的稀疏潜在向量被VAE解码器解码为稀疏SDF，再通过Marching Cubes算法提取出高保真的3D网格。
*   **多视图与场景扩展**：
    *   **多视图生成**：对于多个已知相机姿态的输入视图，分别计算每个视图的3D特征体积，然后在体素级别进行**简单平均**，融合后的特征体积作为条件信号，自然地统一了单视图和多视图生成。
    *   **场景生成**：提出一个模块化管线。首先使用SAM3分割物体，并用图像编辑模型补全被遮挡区域；然后对每个物体用Pixal3D进行像素对齐生成；最后，由于所有物体都已对齐到同一相机空间，仅需通过预测的全局点云图求解物体的相对尺度和深度，即可完成高保真、物体分离的3D场景合成。

### 3. 实验设计
*   **数据集**：
    *   **训练**：Objaverse数据集的TRELLIS-500K子集。
    *   **评估**：
        *   **Toys4K数据集**：用于定量评估单视图和多视图生成的几何精度。
        *   **自采数据集**：从互联网和AI生成源收集的150张包含复杂细节的图像，用于评估开放世界下的图像-3D一致性，并通过用户调研进行主观评价。
*   **对比基准方法**：
    *   **单视图生成**：TRELLIS, TripoSG, Hunyuan3D-2.1, Direct3D-S2。
    *   **多视图生成**：VGGT（代表重建方法），TRELLIS（多视图版本）。
    *   **场景生成**：SAM3D。
*   **评估指标**：
    *   **像素对齐度评估**：在Toys4K上，将生成的网格法线贴图渲染到输入视角，与真值法线图计算IoU、PSNR、SSIM、LPIPS以及角度误差。
    *   **图像-3D语义一致性**：在自采数据集上，使用ULIP2和Uni3D分数评估。
    *   **用户调研**：在自采数据集上，邀请30名参与者对“保真度”和“整体质量”进行1-5分的主观评分。
    *   **多视图几何精度**：使用Chamfer Distance, Earth Mover’s Distance, F-Score。

### 4. 资源与算力
*   论文在“Implementation Details”一节提到了训练细节，例如分阶段训练（256, 384, 512, 1024分辨率）和不同的迭代次数（如密集DiT训练300k+200k次迭代），但**未明确说明所使用的GPU型号、数量以及总训练时长**。

### 5. 实验数量与充分性
*   **实验数量**：论文包含了较为全面的实验组，包括：
    *   单视图生成定量与定性对比（在1个数据集、1个自采测试集上与4种方法对比）。
    *   多视图生成在2、4、6个视图下的定量与定性对比（与2种方法对比）。
    *   场景生成的定性对比（与1种方法对比）。
    *   消融实验，验证了“反投影条件”和“多尺度特征上采样”两个关键模块的作用。
*   **充分性与客观性**：实验设计**较为充分和客观**。
    *   **公平性**：在Toys4K评估中，对基线方法使用了真实相机姿态进行渲染，而Pixal3D利用其像素对齐特性直接投影，保证了比较的公平性。
    *   **多维评估**：结合了像素级几何指标、语义一致性指标和用户主观评价，从多个维度论证了方法的优越性。
    *   **消融验证**：清晰展示了核心设计组件的必要性。

### 6. 论文的主要结论与发现
*   **像素对齐生成范式是可行且可扩展的**。Pixal3D首次大规模证明了直接在像素空间生成3D资产的有效性。
*   **显式对应显著提升保真度**。用反投影条件机制替换跨注意力，建立显式的2D-3D映射，是解决保真度瓶颈的关键，其生成结果在像素对齐度上**接近重建水平**。
*   **范式统一且易扩展**。该方法不仅适用于单视图，还能通过简单的特征体素平均无缝扩展到多视图生成，并为实现高保真、物体分离的3D场景合成提供了简洁有效的新思路。

### 7. 优点
*   **思路创新且深刻**：准确诊断出“隐式对应”是保真度不足的根本原因，并从3D重建的“显式对应”中寻求解决方案，方法论视角独到。
*   **设计简洁有效**：反投影条件方案实现直接，不引入复杂的网络结构或损失函数，却带来了显著的保真度提升，即插即用特征明显。
*   **实验扎实全面**：对比方法具有代表性，评估指标多维（像素对齐度、语义一致性、用户调研），且消融实验有力地支撑了核心设计。
*   **扩展性强**：自然地统一了单视图与多视图任务，并为场景生成提供了比预测物体7自由度位姿更优雅稳定的替代方案，展现了强大的生命力。

### 8. 不足与局限
*   **算力信息缺失**：论文未披露训练所需的GPU资源、型号和具体的训练时间，不利于精确评估其计算成本和复现门槛。
*   **对像素噪声敏感**：反投影机制将像素与3D紧密捆绑，使得分割边缘等像素级的噪声可能被放大，在生成的3D模型上产生几何伪影。
*   **多视图依赖相机姿态**：当前的多视图扩展方案要求输入视图具备已知且相对准确的相机参数，这限制了其在无约束图片上的直接应用。
*   **场景管线条依赖**：场景生成流程中的2D遮挡补全步骤是一个潜在的误差来源，在处理复杂遮挡时可能引入错误，影响最终场景的合理性。

（完）
